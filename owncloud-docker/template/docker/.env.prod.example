# {{ project_name }} - Production Environment Variables
# ⚠️ DEPRECATED: This file is retained for reference only.
# Production deployments use Infisical for all secrets (ADR-006).
# See compose.prod.yaml for the Infisical injection pattern.

# Non-secret configuration only:
DOMAIN={{ domain }}
OCIS_IMAGE=owncloud/ocis:7.1.1
CADDY_IMAGE=reg.mini.dev/caddy:2
ENABLE_INFISICAL=true
INFISICAL_ENVIRONMENT=prod
