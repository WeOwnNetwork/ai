# sso — site-level operator config
# Sourced automatically by deploy.sh, backup.sh, restore.sh.
# Commit this file. It contains no secrets — only IDs and slugs.

INFISICAL_PROJECT_ID=117b72e5-c084-44f6-9393-f5252b5ae0a8
INFISICAL_ENV=prod
