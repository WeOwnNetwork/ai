# WeOwn AI Repository Changelog

This file tracks **repository-level** (infrastructure, workflows, governance, cross-cutting documentation) changes.

Application-specific changes live in per-directory CHANGELOGs. See the index below.

**Format**: [Keep a Changelog 1.1](https://keepachangelog.com/en/1.1.0/)
**Versioning**: Per [VERSIONING_WEOWNVER.md](docs/VERSIONING_WEOWNVER.md) — `vSEASON.MONTH.WEEK.ITERATION`

---

## Per-Directory Changelog Index

| Component | Changelog |
| --- | --- |
| AnythingLLM | [`anythingllm/CHANGELOG.md`](anythingllm/CHANGELOG.md) |
| Matomo | [`matomo/CHANGELOG.md`](matomo/CHANGELOG.md) |
| n8n | [`n8n/CHANGELOG.md`](n8n/CHANGELOG.md) |
| Nextcloud | [`nextcloud/CHANGELOG.md`](nextcloud/CHANGELOG.md) |
| Sandbox Docker | [`sandbox-docker/template/CHANGELOG.md.jinja`](sandbox-docker/template/CHANGELOG.md.jinja) |
| SearXNG Docker | [`searxng-docker/template/CHANGELOG.md.jinja`](searxng-docker/template/CHANGELOG.md.jinja) |
| SigNoz Docker | [`signoz-docker/template/CHANGELOG.md.jinja`](signoz-docker/template/CHANGELOG.md.jinja) |
| Vaultwarden | [`vaultwarden/CHANGELOG.md`](vaultwarden/CHANGELOG.md) |
| WordPress | [`wordpress/CHANGELOG.md`](wordpress/CHANGELOG.md) |
| WordPress Dev | [`wordpress-dev/docs/CHANGELOG.md`](wordpress-dev/docs/CHANGELOG.md) |

---

## [Unreleased]

Changes in this section will be promoted to a dated release entry on merge to `main`.

### Added

- **`anythingllm-docker/sites/s004.ccc.bot/` — INT-S004 recovery rebuild (2026-06-02, #WeOwnVer v4.1.1.1)** — a fresh AnythingLLM droplet replacing the locked-out `s004.ccc.bot`. Re-rendered from the `anythingllm-docker` copier template and validated by diff against the hardened `sites/ai.weown.agency/` (INT-P01) — at render time its deployable files matched `ai.weown.agency` modulo names (it has since been hardened further — see the Changed entries) (`project_name=int-s004-anythingllm`, `domain=s004.ccc.bot`, image `reg.mini.dev/anythingllm:1.7.2` — same pin as the source box, so the restored storage needs no schema migration). Single-host Caddyfile (`s004.ccc.bot`, same hostname as the box it replaces), a **dedicated s004 Infisical project** (least privilege, distinct from INT-P01's `weown-anythingllm`), a `${JWT_SECRET:?...}` fail-loud compose guard, and `scripts/bootstrap-s004-infisical.sh` (`read -rs` → push to Infisical, no disk). Ships a recovery `MIGRATION_RUNBOOK.md` (restore the off-box `s004_storage_<TS>.tar.gz` export). Closes the two old failures: `JWT_SECRET` unset on a container restart, and the never-installed daily backup cron.
- **`.github/ADR-005-int-p01-doks-retirement.md` — Proposed ADR (2026-05-25)** documenting the INT-P01 retirement from DOKS, the parallel-build + DNS-cutover migration pattern, two-gate validation (Jason/Yonks staging soak; CTO production-cutover approval), and compliance mappings across NIST CSF 2.0, SOC 2, ISO/IEC 27001:2022, ISO/IEC 42001:2023, CIS Controls v8. Status moves Proposed → Accepted at close of the 7-day post-cutover soak.
- **`anythingllm-docker/sites/ai.weown.agency/` — INT-P01 DOKS → Docker migration site (2026-05-25)** — generated from the `anythingllm-docker` copier template with `project_name=int-p01-anythingllm`, `domain=ai.weown.agency`, `anythingllm_image=reg.mini.dev/anythingllm:1.7.2` (WeOwnLLM hardened, per D381; pinned to the version verified on s004.ccc.bot). Adopts the Path C + Layer 2 bootstrap pattern from [`docs/INFRA_BOOTSTRAP_PATTERN.md`](docs/INFRA_BOOTSTRAP_PATTERN.md): slim cloud-init that hands off to `ansible/deploy.yml`, Layer 2 bootstrap-secret rotation, Layer 1 DO-Spaces remote tofu state, dual-hostname Caddyfile (`ai-stage.weown.agency, ai.weown.agency`) so the production cutover is a DNS A-record swap with no re-deploy. Includes phased migration runbook (`MIGRATION_RUNBOOK.md`) with an optional local-laptop dry-run phase, and a one-shot bridge script `scripts/migrate-from-doks.sh` that streams `/app/server/storage` out of the DOKS pod via `kubectl exec` and wraps it in the skinny-backup tarball layout the existing `scripts/restore.sh` already understands. Source plan: D383 / Tuleap A174 ([#1238]) / Signal `♾️ WeOwn.Dev` 2026-05-21 ask from Jason.
- **`anythingllm-docker/sites/README.md`** — new directory-level README documenting the `sites/` pattern (matching the existing [`keycloak-docker/sites/README.md`](keycloak-docker/sites/README.md) convention), with a current-deployments table and a "creating a new site" walkthrough.
- **`sandbox-docker/` adopts canonical Layer 1 + Layer 2 + Path C (2026-05-26)** — migrated the AIO Sandbox copier template to the WeOwn bootstrap pattern defined in [`docs/INFRA_BOOTSTRAP_PATTERN.md`](docs/INFRA_BOOTSTRAP_PATTERN.md). Adds DO Spaces remote state (`backend.tf.jinja` + `init.sh.jinja`), embedded `rotate-bootstrap-secret.sh` in cloud-init (mints v2 Infisical Machine Identity client secret via Universal Auth API on first boot, atomically swaps `.infisical-auth.env`, revokes v1), thin cloud-init + `ansible/deploy.yml.jinja` app layer with `community.docker.docker_image_pull` (CLI-based; no python3-docker SDK dependency), auto-tagging via `scripts/tag-droplet.sh` (`commit-<sha>`, feature tags), `ssh_source_cidrs` firewall pinning, Caddy file logging with `/var/log/caddy` bind mount for otel-agent ingestion, and pinned `sandbox_image` default (`ghcr.io/agent-infra/sandbox:0.4.0`, no more `:latest`). Switches Infisical CLI install from legacy `install-cli.sh` (capped at v0.38) to the `artifacts-cli.infisical.com` apt repo.
- **`scripts/bootstrap-otel-agent.sh` (2026-05-23)** — one-time per-droplet bootstrap for the OTel fleet agent. Installs the Infisical CLI if missing and writes `/opt/otel-agent/.infisical-auth.env` (0600 root) with the Machine Identity credentials for the Infisical `otel` project. Supports `--droplet <name>`, `--tag <tag>`, and `--host <user@ip>` selectors. Validates the Machine Identity can log in before exiting. Compliance: NIST PR.DS, CIS 3.11, ISO A.5.16.
- **`otel-agent/README.md` (2026-05-23)** — full operator runbook for the SigNoz Cloud observability path: architecture diagram, secrets model (Infisical runtime injection, never on disk), safety notes for existing droplets (all mounts read-only, loopback health port, 256MB cap), per-droplet bootstrap + deploy steps, verification checklist, key rotation procedure, troubleshooting table, compliance mappings.

### Changed

- **Infisical-native OpenTofu provisioning (2026-06-02)** — added `terraform/itofu.sh` (+ template) that runs `tofu` under `infisical run` against a separate **operator-only `weown-tofu`** Infisical project, so the `TF_VAR_*` infra secrets (DO API token, Spaces keys, droplet Machine Identity) never touch a committed file, a local `terraform.tfvars`, or the s004 **app** project (whose Machine Identity the droplet can read). `init` forwards the Spaces creds to the S3 backend. `terraform.tfvars` + `init.sh` remain a gitignored fallback. Runbook Phase 1 documents the `weown-tofu` setup + the isolation rationale.
- **Team SSH access standard for AnythingLLM ops boxes (2026-06-02)** — `ansible/deploy.yml` (+ the template) writes the team's public keys from the Infisical `OPS_AUTHORIZED_KEYS` var to root's `authorized_keys.weown-ops` (managed **exclusively**, alongside the untouched DO break-glass key) and adds it to sshd's `AuthorizedKeysFile` via a drop-in. Grant/revoke = edit `OPS_AUTHORIZED_KEYS` + re-run `deploy.sh` (revoke-on-termination supported); `sshd -t` gates the reload so a bad config can't lock the box. `scripts/bootstrap-s004-infisical.sh` prompts for the key list. Piloted on `s004.ccc.bot`.
- **Standardized DO Spaces bucket names (2026-06-02)** — `weown-terraform-state` → `weown-prod-state` (state) and `weown-backups` → `weown-prod-backups` (backups), in the template + `s004.ccc.bot`. Other sites/services keep the old names (separate coordinated migration to avoid orphaning live state).
- **Renamed terraform var `minimus_token` → `do_token` in `anythingllm-docker` (2026-06-02)** — the variable feeds `provider "digitalocean" { token = ... }`, so it is the DigitalOcean API token (droplet host), **not** a Minimus credential; the Minimus image-registry token is the separate optional `MINIMUS_TOKEN` Infisical secret (`reg.mini.dev` pulls). Renamed in the template `.jinja` + the `s004.ccc.bot` site (+ its runbook `TF_VAR_do_token`), with the distinction documented in the tfvars example. `ai.weown.agency` and the retired `s004/` keep `minimus_token` for now (renaming a live site's var breaks its local `terraform.tfvars` — separate coordinated change).
- **Fail-loud `JWT_SECRET` guard + `/api/ping` caveat across `anythingllm-docker` (2026-06-02)** — added a `${JWT_SECRET:?...}` guard to the template (`anythingllm-docker/template/docker/compose.prod.yaml.jinja`) and both existing sites (`ai.weown.agency` and the retired `s004`), so a `docker compose up` that bypasses `infisical run` **refuses to start** instead of serving broken auth (the INT-S004 lockout mode). Each README now notes that `/api/ping` returns 200 even when auth is broken, so health checks must not rely on it alone. The retired `s004/` (`s004.ccc.bot`) row in `anythingllm-docker/sites/README.md` is marked superseded by `s004.ccc.bot/`.
- **Observability pivot to SigNoz Cloud (2026-05-23)** — per CTO direction, WeOwn AI now sends telemetry to **SigNoz Cloud** (Yonks' managed account), not a self-hosted SigNoz droplet. The `signoz-docker/` copier template is preserved as an optional/fallback path only — see [`signoz-docker/README.md`](signoz-docker/README.md) for the status banner. `OTEL_URL` + `OTEL_KEY` live in the Infisical `otel` project and are injected at every `docker compose up` via `infisical run` (never stored on disk; container restarts pick up rotated values automatically).
- **`otel-agent/config.yaml` rewritten for SigNoz Cloud (2026-05-23)** — exporter switched from in-VPC `otlp` (insecure) to `otlphttp/signoz` with TLS + `signoz-access-token` header reading `${env:OTEL_KEY}`; endpoint reads `${env:OTEL_URL}`. **OTLP/HTTP chosen over gRPC** so the Infisical-stored `OTEL_URL` (a full `https://ingest.us2.signoz.cloud` URL) works as-is — the gRPC exporter requires bare `host:port` form. SigNoz Cloud destination region is **us2** (not us1 or us). Health endpoint moved from `0.0.0.0:13133` → `127.0.0.1:13133` (loopback only) so `network_mode: host` does not expose it. Added `filter/skip_self` processor to prevent the otel-agent's own logs creating a feedback loop. Added `sending_queue` for buffering during transient SigNoz Cloud unavailability. Stricter filesystem exclude list (avoids container/snap pseudo-filesystems). Compliance: NIST DE.CM, CIS 8.5, ISO A.8.15.
- **`otel-agent/compose.yaml` rewritten for Infisical runtime injection (2026-05-23)** — `OTEL_URL` and `OTEL_KEY` are now read from the environment with `:?` shell-fail-if-missing guards, so a misconfigured `infisical run` fails fast with a clear message instead of silently shipping no data. Health probe target updated to `127.0.0.1:13133`. JSON-file log driver pinned with rotation caps (10MB × 3 files). All host mounts remain read-only.
- **`otel-agent/deploy.yml` rewritten (2026-05-23)** — removed broken Jinja escaping pattern (`{{ '{{' }} … {{ '}}' }}`) that Gemini had introduced; these are plain Ansible playbooks, not copier templates. Now requires `<otel_agent_dir>/.infisical-auth.env` to exist (created by `bootstrap-otel-agent.sh`) and starts the container under `infisical run` using the bootstrapped Machine Identity. Path is configurable via `-e otel_agent_dir=...` (default `/opt/otel-agent`; burnedout-xyz uses `/root/observability/otel-agent`). Infisical env slug is configurable via `-e infisical_env=...` (default `dev` to match the existing "Development" environment in the `otel` project). Added pre-flight checks (docker present, infisical CLI present, auth file present). Added wait-for-health task.
- **`scripts/bootstrap-otel-agent.sh` adds `--dir` and `--env-slug` flags (2026-05-23)** — `--dir <path>` overrides the install directory (default `/opt/otel-agent`; burnedout-xyz uses `/root/observability/otel-agent`). `--env-slug <slug>` records the Infisical env to use at runtime in the auth file (default `dev`); the deploy script reads this back so the env-slug only needs to be specified once during bootstrap.
- **`scripts/deploy-otel-fleet.sh` rewritten + `--dir` / `--env-slug` flags (2026-05-23)** — dropped the `<signoz-private-ip>` positional argument (no longer applicable with SigNoz Cloud). Now supports `--droplet <name>`, `--tag <tag>`, and `--host <user@ip>` selectors, plus `--dir`, `--env-slug`, `--environment`, `--namespace`, `--dry-run` options. `--env-slug` is optional and overrides the value recorded by bootstrap. Refuses to deploy to a host that has not been bootstrapped at the specified `--dir`. Wraps `docker compose up` in `infisical run` for runtime secret injection. Includes inline health wait (up to 60s).

### Deprecated (kept for fallback only)

- **`signoz-docker/` (self-hosted SigNoz copier template)** — superseded by SigNoz Cloud + `otel-agent/`. Files retained, deployment paused. Reactivation requires an ADR; see [`signoz-docker/README.md`](signoz-docker/README.md) for the procedure.

### Fixed

- **cloud-init `$$` over-escaping broke Layer-2 rotation; `awscli` apt-install errored the bootstrap (2026-06-02)** — two more `anythingllm-docker` cloud-init bugs found bootstrapping INT-S004. (1) The embedded `rotate-bootstrap-secret.sh` used `$$VAR`/`$$(…)`, assuming Terraform `templatefile()` collapses `$$`→`$`; it does **not** (only `$${`→`${`), so the literal `$$` became the bash PID and the rotation silently failed on every deploy (it is best-effort/exit-0, so it went unnoticed) — the v1 bootstrap secret was never rotated. Fixed `$$VAR`→`$VAR` / `$$(…)`→`$(…)`, keeping the legitimate `$${…}` escapes. (2) `awscli` in the apt `packages:` list fails to install on current Ubuntu, which set `cloud-init status: error` and skipped the rest of `runcmd`; replaced with the official AWS CLI v2 installer in `runcmd` (best-effort, non-fatal) and added `unzip`. Applied to the template + both sites (`s004.ccc.bot`, `ai.weown.agency`); rendered YAML validated and the rotation script passes `bash -n` after render. Also corrected the now-known-wrong `$$` guidance in `CLAUDE.md`.
- **cloud-init silently skipped its entire bootstrap on non-ASCII characters (2026-06-02)** — the `anythingllm-docker` cloud-init template and both rendered sites (`s004.ccc.bot`, `ai.weown.agency`) had em-dashes (`—`), arrows (`→`), and a section sign (`§`) in comments and log strings. The em-dash's UTF-8 middle byte is `0x80`, a C1 control char that DigitalOcean's ConfigDrive → cloud-init YAML 1.1 parser rejects (`unacceptable character #x0080`), so the whole cloud-config was discarded as "empty cloud config" — Docker, the Infisical CLI, `.infisical-auth.env`, and the Layer-2 secret rotation never ran (`cloud-init status` = `degraded`, `/opt/<project>/` absent). Scrubbed all three cloud-init files to pure ASCII (`—`→`-`, `→`→`->`, `§`→`section`); the template re-renders byte-identical to the site. cloud-init user-data must be ASCII-only. Caught bootstrapping the INT-S004 rebuild.
- **`itofu.sh` + `restore.sh` header inconsistencies (2026-06-02)** — `itofu.sh` (template + `s004.ccc.bot`) now lists `TF_VAR_alert_email` (a DO-verified email) in its "Required Infisical secrets" header, with the `TF_VAR_enable_monitoring=false` opt-out, matching the runbook/tfvars and closing the `email is not verified` apply footgun. `restore.sh` (template + site) no longer advertises an `s3://…` path argument it would actually reject — the header now documents that the input is the backup *name* only (validated `^[A-Za-z0-9._-]+$`) and that the tarball is auto-fetched from DO Spaces when absent locally. Both scripts were also scrubbed to pure ASCII. Caught by Copilot review on PR #39.
- **`OPS_AUTHORIZED_KEYS` sync could revoke all team SSH access on a blank value (2026-06-02)** — the ansible SSH-key sync task guarded only against an unset/empty `OPS_AUTHORIZED_KEYS` (`[ -z ]`), but a whitespace-only value passed that check and then had every line stripped by the blank-line filter, producing an empty temp file that `install` would write over `authorized_keys.weown-ops` — wiping all team keys and locking ops out (the DO break-glass key, managed separately, would survive). Reordered to filter first and treat "no non-blank lines" the same as unset (warn + exit without touching the destination). Fixed in the template (`ansible/deploy.yml.jinja`) and the `s004.ccc.bot` site; verified with a harness (unset/empty/whitespace-only all preserve the existing file; real keys still sync). Caught by Copilot review on PR #39.
- **`TF_VAR_alert_email` missing from the `weown-tofu` setup caused a `tofu apply` failure (2026-06-02)** — the `s004.ccc.bot` runbook's `weown-tofu` `TF_VAR_*` table omitted `alert_email`, so `var.alert_email` fell back to its placeholder default `alerts@example.com` and DigitalOcean rejected the CPU/mem/disk monitor-alert creation with `400 … email is not verified` (the rest of the apply — droplet, reserved IP, firewall — succeeded). Added `TF_VAR_alert_email` to the runbook table with a note that it must be a DO-**verified** email (plus a `TF_VAR_enable_monitoring=false` opt-out), and added the same ⚠️ warning to `terraform.tfvars.example` in both the `s004.ccc.bot` site and the template `.jinja`.
- **`OPENROUTER_API_KEY` never injected into the AnythingLLM container across `anythingllm-docker` (2026-06-02)** — the compose files documented `OPENROUTER_API_KEY` as a required Infisical secret, but the `environment:` block only injected `JWT_SECRET` and `ADMIN_EMAIL` (the original Helm chart mounted all three). A redeploy/restore therefore came up with no OpenRouter credential in the container env — and for the INT-S004 restore specifically, AnythingLLM would fall back to the **expired** key persisted in the restored `storage/.env`, breaking inference with an opaque OpenRouter 401. Added a fail-loud `${OPENROUTER_API_KEY:?...}` injection (same pattern as the `JWT_SECRET` guard) to the template (`compose.prod.yaml.jinja`) and both live sites (`s004.ccc.bot`, `ai.weown.agency`); a value injected via `infisical run` now correctly overrides the stale persisted key. Also gave `OPENROUTER_MODEL_PREF` a default (`${OPENROUTER_MODEL_PREF:-anthropic/claude-opus-4.5}` in the sites, `{{ openrouter_model_pref }}` in the template) to silence the Compose "variable is not set" warning. Caught by Copilot review on PR #39.
- **`otel-agent/deploy.yml` broken Jinja escaping (2026-05-23)** — the file is a plain Ansible playbook (not a copier template), but it had been written with `{{ '{{' }} otel_agent_dir {{ '}}' }}` patterns that would render as the literal string `{{ otel_agent_dir }}` and prevent Ansible variable substitution. Replaced with plain `{{ otel_agent_dir }}` throughout.

<!-- ── older 2026-05-14 batch (Added / Changed / Fixed continued) ── -->

- **`signoz-docker/` copier template (2026-05-14, Added)** — new copier template for self-hosted SigNoz observability platform (logs + metrics + traces via ClickHouse). 19 files: Docker Compose (6-service stack: ZooKeeper, ClickHouse, SigNoz, OTel Gateway, Caddy), OpenTofu IaC (droplet, reserved IP, firewall with OTLP ports VPC-only), cloud-init bootstrap, Ansible deploy playbook, skinny backup/restore scripts, monitoring alerts. Compliance: NIST DE.CM, CIS 8.2, ISO A.8.15. **Status as of 2026-05-23**: see Deprecated section above — kept as optional fallback only.
- **`searxng-docker/` copier template (2026-05-14, Added)** — restructured SearXNG deployment (previously plain Ansible on `feature/searxng-deployment` branch) into full copier template matching `keycloak-docker` patterns. 20 files: Docker Compose (SearXNG + Valkey + Caddy), OpenTofu IaC, Infisical secrets management, skinny backups, monitoring alerts, browser-search integration playbook (Chrome/Edge/Firefox on Linux + Windows). Resolves PR #20 alignment gaps.
- **`otel-agent/` fleet deployment (2026-05-14, Added)** — OpenTelemetry Collector agent deployed per-host to `/opt/otel-agent/`. Collects: container metrics (docker_stats), host metrics (CPU, memory, disk I/O, filesystem, load), container logs, Caddy access logs, syslog/auth.log. Exports via OTLP gRPC. Original 2026-05-14 design targeted an in-VPC SigNoz gateway; **superseded 2026-05-23 by SigNoz Cloud destination** — see Changed section above.
- **`scripts/deploy-otel-fleet.sh` (2026-05-14, Added)** — fleet deployment script. Original 2026-05-14 design took `<signoz-private-ip>` arg; **superseded 2026-05-23 by Infisical-driven flagged interface** — see Changed section above.
- **`scripts/enable-do-agent.sh` (2026-05-14, Added)** — idempotent script to install free DigitalOcean extended metrics agent (`do-agent`) on all tagged droplets. Provides memory, disk, and load metrics in DO dashboard at zero cost.
- **`anythingllm-docker/ansible/configure-allm.yml` (2026-05-14, Added; moved 2026-05-25)** — idempotent Ansible playbook for Docker-based AnythingLLM instances. Configures MCP servers (SearXNG at `searxng.weown.app`) via `docker exec` + `jq` deep merge. Originally landed under `anythingllm/ansible/` but moved alongside the other Docker-stack assets in `anythingllm-docker/` since `anythingllm/` is reserved for the helm/k8s deployment path. Gemini-style broken Jinja escapes were fixed in PR #26 cleanup.
- **`CLAUDE.md` (2026-05-14, Added)** — project-level Claude Code guide. References `.github/copilot-instructions.md` for compliance standards, documents copier template patterns, branch naming convention, Infisical secrets model, and fleet management scripts.

- **`.github/CODEOWNERS` contributor handoff (2026-05-13, Changed)** — `@romandidomizio` removed from all path assignments (left 2026-05-15); `@dhruvmalik007` references removed (no longer with team). `@ncimino` is now the sole assigned reviewer on every path. `@iamwaseem18` (Mohammed) and `@mshahid538` (Shahid) noted as available at `@ncimino`'s discretion. All `TODO(2026-05-15)` handoff comments removed. Header updated to reflect 1-approval requirement and Roman as previous steward.
- **Required reviewers changed from 2 → 1 (2026-05-13)** — updated across `.github/CODEOWNERS` header, `CONTRIBUTING.md` §6.4 merge requirements, `README.md` Code Review Model, `.github/ADR-004-copilot-auto-review-ruleset.md` CIS Controls v8 18.3 compliance row + Layer 1 pruning criteria reviewer requirement, `.github/workflows/auto-pr-to-main.yml` PR body "Required Reviewers" text, `.github/workflows/README.md` §11 troubleshooting row.
- **`CONTRIBUTING.md` §1 prerequisites + §10 Questions updated (2026-05-13)** — removed `@romandidomizio` and `@dhruvmalik007` references; org-access contact updated to `@ncimino`; process/governance contact updated to `@ncimino` + CODEOWNERS link.
- **`.github/workflows/auto-pr-to-main.yml` reviewer assignment updated (2026-05-13)** — `--add-reviewer ncimino,romandidomizio` → `--add-reviewer ncimino` (both existing PR update + new PR create paths). `TODO(2026-05-15)` comments removed.
- **`.github/ADR-004-copilot-auto-review-ruleset.md` ruleset structure amended (2026-05-01)** — `deletion` rule removed from `~ALL` branches in both Layer 1 (repo-level) and Layer 2 (enterprise-level) rulesets. Retained 2 rules: `non_fast_forward` + `copilot_code_review`. Rationale: post-merge branch deletion was permanently blocked by `deletion` on `~ALL` with empty `bypass_actors` — no one (including org admins) could delete any branch. `main` branch deletion protection remains via ADR-003 rule #11. Decision Log appended. Compliance mappings adjusted (SOC 2 CC6.1, ISO 27001 A.8.13).
- **`CONTRIBUTING.md` §8 troubleshooting expanded (2026-05-01)** — added "My PR is blocked — commits are unsigned" scenario. Removed Option 1 (rebase + force-push) because `non_fast_forward` ruleset on `~ALL` branches blocks force-push. Retained Option 2: close PR, cherry-pick to fresh branch with `git cherry-pick -S`, open new PR. Also added "Copilot review didn't start on my auto-created PR" troubleshooting entry documenting the PR-creation-time caching behavior: Copilot evaluates auto-review at PR-creation time, not push time, so the first commit on a new branch won't trigger review until a subsequent push. All subsequent pushes on an open PR are reviewed automatically. Replaces the older single "I need to sign commits I already pushed unsigned" subsection.
- **Repo Settings → General → Pull Requests (2026-05-01)**: enabled **"Automatically delete head branches"** (merged branches auto-delete, replacing the retired `deletion` rule on `~ALL`); enabled **"Always suggest updating pull request branches"** (keeps PRs current with `main` for cleaner merges).

- **`runcmds:` → `runcmd:` typo in all cloud-init templates (2026-05-14, Fixed)** — cloud-init only recognizes `runcmd:` (singular). The typo `runcmds:` silently caused boot commands to never execute. Fixed in: `keycloak-docker/template/terraform/templates/cloud-init.yaml.jinja`, `anythingllm-docker/template/terraform/templates/cloud-init.yaml.jinja`, `keycloak-docker/sites/sso.weown.dev/terraform/templates/cloud-init.yaml`. New templates (signoz-docker, searxng-docker) use the correct form.
- **`scripts/manage-droplets.sh` SSH key rotation parsing bug (2026-05-14, Fixed)** — `rotate-authorized-keys` used `tr ' ' '\t'` which mangled SSH key names containing spaces. Replaced with per-key-ID loop that fetches Name and PublicKey individually via `doctl compute ssh-key get`.

- **Copilot PR #17 round-2 comment fixes (2026-05-13, Fixed)** — addressed remaining Copilot comments from the 2026-05-13 push:
  - **`workflows/README.md` stale "2 reviewer" references** (comment 3236477797): updated 5 remaining "2 reviewers/approvals" → "1 reviewer/approval" at §6 PAT rotation step 13, §8.1 rule #1 table row, §8.1 "Interaction with workflows", §9 step 4, §10 item #7.
  - **`workflows/README.md` §9 Reviewer Rotation**: updated step 1 to reflect handoff complete (2026-05-15, PR #17); removed `@dhruvmalik007` from the done-placeholder list; `--add-reviewer ncimino` only.
  - **`workflows/README.md` §10 Transition Checklist**: marked items 1 (PAT stewardship), 4 (CODEOWNERS), 5 (workflow reviewer) as ✅ done; updated owners to reflect `@ncimino` and `@YonksTEAM` post-transition; removed `@romandidomizio` from action-owner column.
  - **`workflows/README.md` §11 line 587 duplicate link** (comment 3236447687): removed redundant second `[ADR-004]` link in the "No Copilot review on first commit" row; kept single `[ADR-004 § Empirical Validation Results]` link with correct anchor.
  - **`CODEOWNERS` risk-acceptance note** (comment 3236477912): added explicit `RISK ACCEPTANCE` block in the CODEOWNERS header documenting the deliberate 2-reviewer → 1-reviewer posture change, compensating controls (Copilot AI review + CodeQL), and trigger for revisiting.

- **Copilot PR #17 round-1 comment fixes (2026-05-13, Fixed)** — addressed all open Copilot review comments on branch `feature/roman-ruleset-cleanup-and-docs`:
  - **`CONTRIBUTING.md` §8 Cause B force-push** (comments 3175275840 + 3175470086): removed `git push --force-with-lease` step; replaced with note that force-push is blocked by `non_fast_forward` and directs users to the close+recreate path already documented in the same §8 section.
  - **`ADR-004` title** — removed "Deletion Protection" from title since the `deletion` rule was removed from `~ALL`; title now accurately reflects the 2 active rules.
  - **`ADR-004` Verification Procedure expected output** (comment 3175470115): updated Layer 1 expected `rules` array from `["deletion", "non_fast_forward", "copilot_code_review"]` → `["non_fast_forward", "copilot_code_review"]`; updated Layer 2 expected count from "identical 3 rules" → "identical 2 rules".
  - **`ADR-004` Decision Log** (comment 3175275917): removed reference to gitignored `PR7_HANDOFF_CHECKLIST.md` in the round-7 close-out entry.
  - **`auto-pr-to-main.yml` NOTE** (comment 3175470133): rewrote NOTE to clarify the push-before-PR pattern specific to this workflow — commits are pushed before the PR exists, so `review_on_push: true` has no new push delta at PR-creation time; first Copilot review fires on the next push to the open PR. Reconciled with ADR-004 § Empirical Validation Results.
  - **`workflows/README.md` §11 Copilot row** (comment 3175470144): updated "No Copilot review on first commit of auto-created PR" row to clarify this is specific to the `auto-pr-to-main.yml` push-before-PR pattern (not all PRs), and that manually-created PRs do get Copilot at PR-creation time.

---

## [v3.3.5.1] — 2026-04-27 to 2026-04-28

Continuation iteration of the in-flight PR #13 (originally tagged `[v3.3.4.2]` below). Bumped to `v3.3.5.1` because this section captures the portion of the PR that moved into the 5th week of April under the active #WeOwnVer interpretation (`vSEASON.MONTH.WEEK.ITERATION` → Season 3, April, Week 5, Iteration 1). The original 2026-04-23 work + Copilot rounds 1–3 (2026-04-24) remain under `[v3.3.4.2]` below; the later continuation rounds documented here span 2026-04-27 (rounds 4–11) through 2026-04-28 (rounds 12–13). The PR itself remains a single squash-merge unit; the CHANGELOG split is purely about #WeOwnVer fidelity per the active versioning calendar. Version stays at `v3.3.5.1` across the day boundary because the PR is a single semantic unit (no fresh iteration started post-merge).

### Added

- **`.github/ADR-004-copilot-auto-review-ruleset.md`** — new ADR (added 2026-04-27 in round-6 close-out, refined in round-7) documenting the **two-layer defense-in-depth ruleset** that targets `~ALL` branches with three rules: `deletion`, `non_fast_forward`, and `copilot_code_review`. **Layer 1**: repo-level "Copilot auto-review" ruleset (id `12131972`, configured 2026-04-23). **Layer 2**: enterprise-level ruleset configured 2026-04-27. Both layers enforce identical rules; the union-of-rules semantics provides defense-in-depth that survives enterprise migrations / SKU changes / individual misconfigurations and mirrors WeOwn's branch-name regex defense-in-depth pattern. **Compliance mappings**: SOC 2 CC6.1, CC7.1, CC7.2, CC8.1; ISO/IEC 27001:2022 A.8.32, A.8.13; ISO/IEC 42001:2023 A.6.2.7, A.6.2.8; NIST CSF 2.0 PR.IP-3, DE.CM-1; CIS Controls v8 18.3. Includes explicit pruning criteria for Layer 1 (do not delete unless ALL of: 5+ consecutive bot-authored PRs auto-trigger Copilot via Layer 2 alone + no enterprise migration planned + Layer 2 deletion+non_fast_forward verified by deliberate test + reviewer sign-off). Includes end-to-end auto-trigger validation procedure pointing at the ADR's own Decision Log table for in-repo logging (replaces a transient reference to a gitignored operational checklist; resolves Copilot R7 #5). Includes an **Empirical Validation Results** section documenting the round-7 controlled experiment finding: Layer 2 + Copilot Business entitlement DO work for newly-created PRs but Copilot auto-trigger is evaluated at PR-creation time, not push time — pre-existing PRs (created before the entitlement was provisioned) will never auto-trigger and the only remediation is close+re-open (usually not worth the cost).
- **`.github/workflows/README.md` §2A "What `auto-pr-to-main.yml` Does (Step-by-Step)"** — new authoritative narrative walkthrough of the auto-PR workflow, with three subsections: **Trigger matrix** (push + workflow_dispatch + concurrency); **Step-by-step walkthrough** (banners 1–9 with what-it-does + failure-modes columns); **Why the team benefits** (SOC 2 CC8.1 evidence narrative, zero-friction onboarding, Copilot AI review quality, defense-in-depth pattern). Inserted between §2 and §3 to give workflow-level context before the attribution model details. TOC updated.
- **`.github/workflows/README.md` §11 "Troubleshooting — Symptom → Cause → Verification"** — consolidated symptom/cause/verification table covering all three workflows (`auto-pr-to-main.yml`, `branch-name-check.yml`, `pat-health-check.yml`) plus Copilot auto-review, branch protection / rulesets, and Infisical sync drift. Old §11 ("Related Documents") renumbered to §12. TOC updated. Closes the long-standing v3.3.4.3 queue item to add troubleshooting documentation; accelerated into this PR per user direction.

### Changed

- **`.github/workflows/auto-pr-to-main.yml` step 7 contributor aggregation** — replaced the `while read -r count handle` pipeline with `awk '{count=$1; $1=""; sub(/^[[:space:]]+/, "", $0); ...}'`. The `read`-based approach split on whitespace and silently truncated multi-word names in the fallback path (e.g., `Jane Doe` rendered as `- Jane (3 commits)` instead of `- Jane Doe (3 commits)`). The `awk`-based approach preserves the entire remainder of the line after the count. Includes an inline comment explaining the rationale so future maintainers don't regress. Resolves Copilot R7 comment #8. (The fallback path triggers when `gh api /commits/{sha}` returns a null login — typical for unlinked external commits.)
- **`auto-pr-to-main.yml` step 6 attribution comment** — softened the immutability claim from "first commit is immutable under the `non_fast_forward` ruleset" (under-specified) to "stable across pushes because the 'Copilot auto-review' ruleset (id 12131972, see ADR-004) enforces `non_fast_forward` on `~ALL` branches in this repo, blocking the rebase / force-push that would change the first-commit identity". Resolves Copilot R6 comment A by making the enforcement chain auditable. Identical clarifications applied to `workflows/README.md` §3 Opened-by table row (Copilot R6 D) and `CONTRIBUTING.md` §4 Opened-by bullet (Copilot R6 C).
- **`auto-pr-to-main.yml` step 7 contributors fallback PII strip** — changed `git log -1 --format='%an <%ae>'` to `git log -1 --format='%an'`. Names are already public on GitHub commit pages; emails are PII per GDPR/CCPA + WeOwn's existing PII-minimization posture (already applied to `CONTRIBUTING.md` handle table + `CODEOWNERS`). Resolves Copilot R6 comment E.
- **`auto-pr-to-main.yml` step 8 Recent Commits PII strip** — changed `git log --format='### %h %s%n%n**Author:** %an <%ae>%n**Date:** %ad...'` to `'**Author:** %an'`. Same principle as step 7 fallback. Reviewers can still cross-reference via the commit SHA + GitHub web UI. Closes the only remaining email exposure path in the PR body. User-directed extension of round-6 PII work.
- **`auto-pr-to-main.yml` step 8 PR body parenthetical descriptions removed** (user-directed UX polish): the `Opened by:`, `Last pushed by:`, and `Contributors on this branch:` lines previously included italic explanatory parentheticals that duplicated information already visible in CONTRIBUTING.md §4 + workflows/README.md §3. Trims visual noise without information loss.
- **`auto-pr-to-main.yml` `workflow_dispatch:` trigger added** — enables manual re-run for debugging / PR-body refresh without needing an empty commit. The defense-in-depth branch-name regex (step 1) makes dispatch on `main` or unconventional branches a safe no-op (`exit 0`). Resolves the 2026-04-23 PAT-rotation scenario that required commit `ba6d7ee` (empty re-trigger commit) — going forward this is one click in the Actions tab.
- **`auto-pr-to-main.yml` `concurrency:` block added** — `group: auto-pr-${{ github.ref }}, cancel-in-progress: true`. Prevents race conditions when developers rapid-push: the older in-flight run is cancelled and only the latest run creates / updates the PR. Per-branch isolation; pushes on different branches don't interfere with each other.
- **PR-body attribution upgrade — three-tier model**: replaced the prior single `**Triggered by:** @<user>` line (semantically ambiguous — implied "opener" but actually showed *most recent pusher*) with three distinct, semantically-precise fields:
  - **`Opened by:`** — GitHub @handle of the FIRST commit's author on the branch. Resolved via `git rev-list --reverse "${GIT_RANGE[@]}" | head -n 1` → `gh api /repos/$GITHUB_REPOSITORY/commits/$FIRST_SHA --jq '.author.login'`. Stable across pushes (see ADR-004 for the enforcement chain). Two fallbacks: (1) `.committer.login` if `author.login` is null, (2) `LAST_PUSHED_BY` if both fail.
  - **`Last pushed by:`** — `${{ github.triggering_actor || github.actor }}`. Updates every push.
  - **`Contributors on this branch:`** — list now uses GitHub @handles with commit counts (`- @romandidomizio (4 commits)`). Per-commit `gh api /repos/.../commits/{sha} --jq '.author.login // .committer.login // ""'` resolution; fallback to commit-author NAME ONLY (no email) for unlinked external commits.
- **`workflows/README.md` §3 rewritten** — (a) intro sentence corrected to clarify attribution comes from GitHub event context + commits API, **not** branch-name parsing (resolves Copilot R5 #1); (b) Parsing Rules retitled "Parsing Rules (branch name → regex validation only)" with 3 simplified steps; (c) new "PR body attribution — three-tier model (by design)" subsection with Field/Value/Source/Updates table; (d) "Branch name vs. PR body" table expanded from 2 rows to 4 (branch `<dev>` + `Opened by:` + `Last pushed by:` + `Contributors on this branch:`).
- **`CONTRIBUTING.md` §4 rewritten** — (a) `<dev>` paragraph updated to clarify attribution uses commits API + event context, not branch parsing; (b) blockquote callout table expanded to 4 rows (matches workflows/README.md); (c) new explanatory bullets for each of the three PR-body fields with their resolution mechanism; (d) `feature/add-thing` trailing note updated; (e) round-5 Copilot comment #2 fix: attribution paragraph rewritten to drop misleading phrasing; (f) round-7 alignment fix: `Contributors:` label updated to `Contributors on this branch:` in both the table cell and the explanatory bullet to match the exact PR-body wording (resolves Copilot R7 #6).
- **`workflows/README.md` header version + Last updated**: `**Version**: v3.3.4.1 → v3.3.5.1`, `**Last updated**: 2026-04-23 → 2026-04-27`. The original 2026-04-23 bump was to `v3.3.4.2` (documented in `[v3.3.4.2]` below); this iteration's continuation work bumps it again to `v3.3.5.1` per the user's #WeOwnVer week-5 interpretation. Resolves Copilot R7 comments #1 + #2 (workflows/README.md header inconsistency) and #7 (CONTRIBUTING.md header inconsistency); same bump applied to `CONTRIBUTING.md` (Last updated 2026-04-23 → 2026-04-27), `.github/ADR-003-main-branch-ruleset.md` (Date "2026-04-23" → "2026-04-23 (ruleset configured) / 2026-04-27 (ADR last revised)" — resolves Copilot R7 #3), and `.github/ADR-004-copilot-auto-review-ruleset.md` Decision Log + Empirical Validation Results section (resolves Copilot R7 #4).
- **`.github/ADR-003-main-branch-ruleset.md` Related section** — added cross-reference to ADR-004 (under Related), making the two-ADR structure discoverable.
- **`.github/ADR-002-infisical-github-sync.md` naming convention revised (2026-04-28)** — the original convention used `WEOWN_BOT_PAT__<ORG>_<REPO>` as the Infisical secret name with the assumption that the GitHub Sync UI exposed a per-secret rename feature at sync-config time. Empirical finding while configuring the sync on 2026-04-28: **Infisical's "Key Schema" can ADD prefixes/suffixes around the `{{secretKey}}` template but cannot STRIP them**, so the original convention is unworkable at sync-creation time. **Revised convention**: Infisical secret name = `WEOWN_BOT_PAT` (identity-mapped, no suffix) + namespace across repos via separate Infisical projects (`weown-bot/<org>-<repo>`), not secret-name suffixing. Updates: (a) Architecture diagram in ADR-002 redrawn to show project-per-scope; (b) Naming Convention section rewritten with explanation of why the original convention fails; (c) Implementation Notes "Initial setup" steps revised; (d) **NEW Decision Log section appended** with 2026-04-23 + 2026-04-28 rows documenting the convention evolution; (e) ADR-002 header `Version` bumped from `v3.3.4.1` to `v3.3.5.1`, `Date` updated to `2026-04-23 (initial) / 2026-04-28 (naming convention revised — see Decision Log)`. Status remains "Accepted" — this is an implementation-detail revision, not a decision reversal. Cross-doc updates: `workflows/README.md` §2.4 Usage Table + §5.1 Onboarding steps 2 + 4 + §6 Rotation step 6 + new §6.1 Sync Options Configuration sub-section. The Sync Options table documents the four configuration parameters (Initial Sync Behavior = Overwrite [forced]; Key Schema = `{{secretKey}}` identity; Disable Secret Deletion = Yes; Auto-Sync Enabled = Yes) with rationale per row.
- **`.github/workflows/README.md` §6.1 NEW: Sync Options Configuration** — step-by-step guidance for configuring an Infisical → GitHub Secret Sync. Covers the four UI options (Initial Sync Behavior, Key Schema, Disable Secret Deletion, Auto-Sync Enabled) plus a Migration Steps sub-section for the 2026-04-28 transition from the legacy `WEOWN_BOT_PAT__WEOWNNETWORK_AI` secret to the revised `WEOWN_BOT_PAT` identity-mapped secret in the new `weown-bot/weownnetwork-ai` Infisical project. Cross-references ADR-002 Decision Log.
- **`.github/workflows/README.md` §2.4 Usage Table refreshed** — active row updated to reflect the 2026-04-28 PAT rotation: `Expiration: 2026-07-22 → 2026-07-27` (90 calendar days from 2026-04-28); `Last Rotated: 2026-04-23 → 2026-04-28`; `PAT Secret (Infisical) column: WEOWN_BOT_PAT__WEOWNNETWORK_AI → WEOWN_BOT_PAT (Infisical project: weown-bot/weownnetwork-ai)`. Placeholder rows for future repos updated to match the new project-per-scope naming.
- **`.github/workflows/pat-health-check.yml` example date refreshed** — line 133 expected-format example string updated from `'2026-07-22 17:13:00 UTC'` to `'2026-07-27 17:13:00 UTC'` for currency. Behavior unchanged (the value is just an example in the diagnostic message; parsing logic is generic).

### Fixed

- **`.github/ADR-004-copilot-auto-review-ruleset.md` validation step** — the auto-trigger validation procedure previously instructed "record the run number + timestamp in `PR7_HANDOFF_CHECKLIST.md`" but that file is gitignored (operational notes; not in repo). Updated to point at this ADR's own Decision Log table for in-repo logging. Resolves Copilot R7 comment #5.
- **CHANGELOG drift on three-tier attribution wording** — the v3.3.4.2 "Added" bullet for platform-sourced attribution (originally written round-1) still described the workflow as producing `**Triggered by:** @<real-github-username>`. Round-6 rewrote it to describe the three independent fields (`Opened by:`, `Last pushed by:`, `Contributors on this branch:`) currently emitted, with a parenthetical noting the round-5 evolution. Resolves Copilot R6 comment B.
- **Round-5 auto-trigger claim REVERTED** (carried forward from round-6, refined in round-7): prior session incorrectly inferred from the `2026-04-27T21:59:30Z` Copilot review timestamp on commit `34f28bf` that auto-trigger had succeeded. User clarified the round-5 review was MANUALLY triggered. Memory + checklist + this changelog corrected. **Round-7 controlled-experiment finding**: a fresh PR opened on 2026-04-27 from an old branch DID auto-trigger Copilot review (`Copilot AI review requested due to automatic review settings`), validating the Layer 2 ruleset + Copilot Business entitlement chain, but PR #13 (created 2026-04-23) does NOT auto-trigger because Copilot's auto-review decision is made at PR-creation time. See ADR-004 § Empirical Validation Results.
- **Round-20 Copilot review fixes** (2026-04-28, 2 Copilot R20 fixes — 1 correctness fix + 1 doc-vs-UI display canonicalization with cross-doc cascade):
  - **R20 #1 / `.github/workflows/README.md` §11 Troubleshooting "Merge blocked with 'requires signed commits'" row** — guidance was **incorrect**. Old text: "Configure commit signing per CONTRIBUTING.md §3; retroactive signing is impossible (would force-push) — add a new signed commit to the branch." This was wrong: adding a new signed commit does NOT fix earlier unsigned commits — the merge will still be blocked because the `main` branch protection requires ALL commits in the PR be signed (not just the most recent). Copilot R20 #1 caught the correctness bug. **Fix** (per Copilot's literal suggested change): rewrote the row to explain that adding a new signed commit does **not** fix earlier unsigned commits, and that because retroactive signing would rewrite history and `non-fast-forward` is blocked on `~ALL` branches by Layer 1 + Layer 2 rulesets, the contributor must recreate the branch/PR with all commits signed (or otherwise ensure every commit in the PR is signed). **Compliance basis**: SOC 2 CC8.1 (incident-response runbooks must give correct remediation; misleading guidance during a merge-blocked incident wastes contributor time and may cause repeated failed remediation attempts); ISO/IEC 27001:2022 A.5.24 (runbook accuracy). **Pattern**: this is a **runbook-correctness bug** that survived 19 prior rounds of Copilot review because the row's narrative *sounded* plausible (the "force-push is blocked" framing was correct; the conclusion "add a new signed commit" was wrong). Lesson: troubleshooting tables describing remediation steps need explicit logic-validation against the underlying enforcement chain (here: branch-protection + ruleset rules), not just internal narrative consistency.
  - **R20 #2 / Folder-path canonical display form across all docs** — Copilot R20 #2 flagged that Infisical folder paths were shown with a trailing slash in several places (e.g., `/WeOwnNetwork-ai/`) but the Sync "Source secret path" was documented without it (`/WeOwnNetwork-ai`). This creates real-world copy/paste configuration errors during incident response and new-repo onboarding when stewards transcribe paths from docs into the Infisical UI. Copilot suggested standardizing — gave a literal "add trailing slashes everywhere" suggestion. **User explicit preference: NO trailing slash** (opposite of Copilot's literal suggestion). Rationale: the Infisical UI's "Source secret path" field accepts the path without trailing slash and mirrors the path verbatim, so the no-slash form is the form that survives the round-trip from docs → UI → docs without transformation. Applied trailing-slash → no-trailing-slash canonicalization across:
    - **`.github/workflows/README.md`** — §2 Scope item 3, §2 Scope item 4 (×2 path examples), §2.4 Usage Table active row + 2 placeholder rows, §4.4 Step B step 3 + step 4, §5.1 Common steps step 2 + step 4, §6 Rotation step 6, §6.1 Sync Options Configuration explanatory paragraph + Migration Steps step 1 + step 4. Total: ~14 path normalizations within a single doc.
    - **`.github/INCIDENT_RESPONSE.md`** — Scenario 6 step 6 single instance (steward-handoff Infisical access verification).
    - **`.github/ADR-002-infisical-github-sync.md`** — Architecture diagram lines 40 + 42 (with right-edge whitespace adjustment to maintain box-border alignment); Naming Convention bullet (line 64) with explicit "canonical form: NO trailing slash, per R20 close-out 2026-04-28" parenthetical added for future authors; Implementation Notes step 2 (line 138).
  - **`.github/ADR-002-infisical-github-sync.md` Decision Log appended with R20 row** (in correct chronological position after R17 + R18, NOT as a replacement). The R20 row documents: (a) the canonical form decision; (b) the explicit PRESERVATION of R17 + R18 historical rows with their original trailing-slash forms per the R19-codified "Historical-narrative preservation in audit trails" rule (modifying the historical text would corrupt the audit trail of the substantive R17 + R18 convention decisions); (c) Status remains "Accepted" — this is a display canonicalization, NOT a convention change (both forms refer to the same Infisical folder); (d) **NEW operational rule codified (R20 close-out)**: when documenting a path-like identifier that users will copy into a vendor UI text field, pick the canonical form that the vendor UI itself uses (or the simpler form when the UI accepts both) and use it consistently across all docs.
  - **Header date bumps** for traceability:
    - `.github/ADR-002-infisical-github-sync.md` Date header: `2026-04-23 (initial) / 2026-04-28 (naming convention revised twice — see Decision Log)` → `2026-04-23 (initial) / 2026-04-28 (naming convention revised twice + canonical no-trailing-slash form for folder paths — see Decision Log)`.
    - `.github/INCIDENT_RESPONSE.md` Date header: appended `R20 trailing-slash canonicalization on Scenario 6 step 6` to the existing R13 + R18 list.
  - **`workflows/README.md` Last updated header** (line 6) was already simply `2026-04-28` — no parenthetical detail to extend, no bump needed.
  - **CHANGELOG `[v3.3.5.1]` § Changed historical bullets PRESERVED** — entries documenting the R17 (project-per-repo `weown-bot/weownnetwork-ai` reference) + R18 (folder-per-repo with trailing slashes) close-outs in their historical narrative form; modifying the original-state references would corrupt the close-out audit trail per the R19 rule.
  - **Files touched (4)**: `.github/workflows/README.md`, `.github/ADR-002-infisical-github-sync.md`, `.github/INCIDENT_RESPONSE.md`, `CHANGELOG.md` (this file). PR7_HANDOFF_CHECKLIST.md (gitignored) updated separately.
  - **Pattern continuation**: R20 #1 surfaced a **runbook-correctness bug** (different drift class from R19's label-citation drift); R20 #2 surfaced **doc-vs-UI display drift** (also distinct from prior rounds). Two new sub-classes of accuracy drift identified within a single round, both caught by Copilot late in the v3.3.5.1 review cycle. Cumulative pattern across rounds 7-20: late-round Copilot reviews continue surfacing new sub-classes of accuracy drift even after 19 prior rounds. Going-forward implication: the v3.3.5.1 review cycle is establishing a stable taxonomy of doc-accuracy-drift classes that future PRs can pre-validate against.

- **Round-19 Copilot review fixes** (2026-04-28, 1 Copilot R19 label-canonicalization fix + 3 same-doc + cross-doc cascades per the R18 #1 "Same-Document Consistency Sweep" rule):
  - **R19 #1 / `.github/workflows/README.md` §3 "Branch name vs. PR body" table row label** — the table row at line 170 was labeled `**PR body \`Contributors:\` list**` while the workflow itself emits `**Contributors on this branch:**`(`auto-pr-to-main.yml` step 8 line 320), and the SAME doc's earlier §3 three-tier table at line 161 already used the canonical `Contributors on this branch` label. Copilot R19 #1 flagged the inconsistency: readers cross-referencing the table against the actual PR body would see different labels and might think the doc is describing a different field than the one rendered. **Fix**: renamed the row label to `**PR body \`Contributors on this branch:\` list**` per Copilot's literal suggested change. **This fix is the canonical example of why the R18 #1 "Same-Document Consistency Sweep" rule matters** — the table row at line 170 said `Contributors:` while line 161 in the same doc already used `Contributors on this branch`; R18's new rule mandated sweeping all earlier sections for the same convention; R19 #1 demonstrates the rule's value by catching exactly the kind of intra-doc drift it was designed to surface. **Compliance basis**: SOC 2 CC8.1 (documented operating procedures must be internally consistent across the same document); ISO/IEC 27001:2022 A.5.37 (no contradictory label citations within an authoritative reference doc); ISO/IEC 42001:2023 A.6.2.7 (AI system control descriptions must accurately match what the system emits — here, the actual workflow output line).
  - **R19 same-doc cascade / `.github/workflows/README.md` §11 Troubleshooting table row** — applied the same fix to the §11 row "PR body shows wrong attribution" which used `Contributors:` in the resolution column when listing the three field-label semantics. Updated to canonical `Contributors on this branch:` for cross-section consistency. (Not Copilot-flagged; caught proactively per the R18 #1 same-doc-sweep rule.)
  - **R19 cross-doc cascade #1 / `CONTRIBUTING.md` §4 explanatory bullet** — line 334 bullet `**\`Contributors:\`**` → `**\`Contributors on this branch:\`**` to match the canonical workflow output. The same doc's earlier inline-quoted table at lines 289 + 294 already used the canonical form; the bullet at line 334 was the only inconsistent label-citation in the doc.
  - **R19 cross-doc cascade #2 / `CONTRIBUTING.md` §4 parenthetical at line 328** — within the "regex-valid but convention-violating" example paragraph, the parenthetical "(The PR body's `Opened by:`, `Last pushed by:`, and `Contributors:` fields will still attribute correctly regardless — see next paragraph.)" listed the three field names but used the shortened form for the third one while the other two used the canonical form. Updated to canonical `Contributors on this branch:` for full intra-paragraph consistency.
  - **`CONTRIBUTING.md` Last updated header bump** — "2026-04-28 (R12 §4 attribution-fallback fix + R13 header date sync)" → "2026-04-28 (R12 §4 attribution-fallback fix + R13 header date sync + R19 §4 `Contributors on this branch:` label canonicalization)" for traceability of the R19 cross-doc cascade.
  - **CHANGELOG `[v3.3.5.1]` line 56 PRESERVED unchanged** — the v3.3.5.1 § Changed entry documenting the R7 fix that renamed `Contributors:` → `Contributors on this branch:` quotes the OLD label as the "before" value in a historical close-out narrative ("`Contributors:` label updated to `Contributors on this branch:`"). Modifying that quoted historical value would corrupt the audit trail of when + why the rename happened. The R10 PII-recursive-quote lesson applies in reverse here: when a CHANGELOG entry documents a label rename, the original-value reference must remain verbatim for the audit-trail entry to retain its meaning. **Compliance basis**: SOC 2 CC7.2 (historical audit trail integrity); ISO/IEC 27001:2022 A.5.28 (records of revision history must accurately reflect the original state being revised).
  - **Files touched (4)**: `.github/workflows/README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `PR7_HANDOFF_CHECKLIST.md` (gitignored). Single commit; squash-merge target preserved.

- **Round-18 Copilot review fixes + Infisical naming convention 2nd revision (folder-per-repo)** (2026-04-28, 4 Copilot R18 doc-consistency fixes + user-driven operational simplification of the R17 naming convention):
  - **R18 #1 / `.github/workflows/README.md` §2 Scope & Principles — convention contradiction with §2.4 Usage Table** — Copilot R18 #1 flagged that §2.4 Usage Table reflected the revised Infisical convention (`WEOWN_BOT_PAT` in per-repo containers) but §2 "Scope & Principles" earlier in the doc still stated the old convention (single project + `WEOWN_BOT_PAT__<ORG>_<REPO>` naming), creating contradictory onboarding guidance. **Fix**: rewrote §2 item 3 ("Centralized storage") to describe the single shared project `weown-bot GitHub PATs` with one folder per target repo (e.g., `/WeOwnNetwork-ai/`), and rewrote item 4 ("Consistent naming") to match the revised convention — Infisical secret per folder = `WEOWN_BOT_PAT` (identity-mapped), namespace via folder path, GitHub Actions secret = `WEOWN_BOT_PAT`. Item 4 also notes the 2026-04-28 ADR-002 Decision Log revision and points at §6.1 Sync Options for the full rationale. **Compliance basis**: SOC 2 CC8.1 (documented operating procedures must be internally consistent across the same document); ISO/IEC 27001:2022 A.5.37 (no contradictory onboarding guidance — onboarding contributors must read one canonical convention).
  - **R18 #2 / `.github/INCIDENT_RESPONSE.md` Scenario 6 step 6 — legacy project-name reference** — Copilot R18 #2 flagged that Scenario 6 (Stewardship Gap) step 6 still referenced the legacy Infisical project name (`weown-bot GitHub PATs` only, no folder qualifier) but this PR's revised convention namespaces per-repo via folders inside that project. The runbook needed to match the new structure so a new steward verifying access during a SEV-4 hand-off would know which folder to check. **Fix**: per Copilot's suggested change, updated step 6 to "Verify new steward has Infisical access (project `weown-bot GitHub PATs`, folder `/WeOwnNetwork-ai/`)". Also bumped the file's Date header to include the R18 update for traceability. **Compliance basis**: NIST CSF 2.0 RS.MA-1 + ISO/IEC 27001:2022 A.5.24 (incident-response runbooks must reference current resource identifiers under stress; legacy references slow steward onboarding during an active incident).
  - **R18 #3 / `.github/ADR-004-copilot-auto-review-ruleset.md` Layer 1 bullet #3 — copilot_code_review claim qualification** — Copilot R18 #3 flagged that the Layer 1 rules bullet #3 stated "auto-request Copilot review on every PR (draft + ready) and on every push to an open PR", which contradicts the same ADR's later § Empirical Validation Results section documenting PR-creation-time eligibility caching (pre-existing PRs do not auto-request on subsequent push). **Fix**: rewritten per Copilot's suggested change to "auto-request Copilot review for newly-created, eligible PRs (draft + ready), and re-request review on later pushes only for open PRs where Copilot was auto-requested at creation"; appended a **Note** that explicitly cross-references § Empirical Validation Results and documents the close+reopen / merge+open-fresh remediation paths for pre-existing PRs. Also bumped the file's Date header to include R18 for traceability. **Compliance basis**: SOC 2 CC7.2 (control-evidence must be internally consistent across rule descriptions and validation results); ISO/IEC 42001:2023 A.6.2.7 (AI system enforcement claims must accurately describe the population they apply to — here, newly-created PRs only).
  - **R18 #4 / `.github/workflows/README.md` header `Last updated` — stale date** — Copilot R18 #4 flagged that the file's header `Last updated: 2026-04-27` was stale because this PR's R17 close-out added 2026-04-28 changes (PAT rotation dates + new §6.1 Sync Options Configuration sub-section). **Fix**: per Copilot's suggested change, bumped to `Last updated: 2026-04-28`. **Compliance basis**: SOC 2 CC8.1 + ISO/IEC 27001:2022 A.5.37 (versioned-doc headers must reflect the most recent edit so auditors can locate the audit-trail entry covering the change).
  - **Folder-per-repo namespacing — second revision of the 2026-04-28 naming convention** (user-directed operational simplification): the R17 first-iteration revision used **project-per-repo** (one Infisical project per target repo, e.g., `weown-bot/weownnetwork-ai`). User feedback during the actual sync configuration on 2026-04-28: a single shared Infisical project with one folder per target repo is operationally cleaner — (a) one project-level RBAC boundary instead of N; (b) one expiration-reminder convention; (c) sibling folders discoverable from the same project landing page; (d) the existing `weown-bot GitHub PATs` project absorbs new repos without new project creation. **Cascade**: ADR-002 architecture diagram redrawn to show project-with-folders; Naming Convention section rewritten with the folder-per-repo model + a "why folder-per-repo, not project-per-repo" comparison; Implementation Notes initial setup steps revised (5 steps → 7 steps to add folder creation); Decision Log appended with a SECOND 2026-04-28 row tagged `(R18)` documenting the project→folder shift while preserving the R17 row for audit trail. `.github/workflows/README.md` updates: §2 Scope items 3+4 rewritten (R18 #1 above); §2.4 Usage Table rows updated to show `Infisical project: weown-bot GitHub PATs, folder: /WeOwnNetwork-ai/`; §4.4 Step B + §4.5 Step C rewritten for folder-based initial setup with explicit Source Path = `/WeOwnNetwork-ai`; §5.1 replication steps 2 + 4 rewritten for folder-based; §6 Rotation step 6 references folder; §6.1 Sync Options Configuration updated with new "Why folder-per-repo, not project-per-repo" sub-section + revised Migration Steps. `.github/INCIDENT_RESPONSE.md` Scenario 6 step 6 updated (R18 #2 above). **Compliance basis**: same as ADR-002 baseline (Infisical-primary-with-GitHub-Sync; Status remains "Accepted") — this is a second implementation-detail revision documenting the operationally-cleaner namespacing axis. **NEW operational rule (R18 close-out)**: when a vendor-feature-driven convention is documented in an ADR, validate the convention against actual UI/API configuration before broad cascade — first-cut conventions may need a second-cut revision once the namespacing primitives are exercised in practice (R17 → R18 here is the canonical example; the R17 project-per-repo convention was correct in principle but operationally suboptimal; R18 folder-per-repo arrived from actual sync-creation feedback within the same day).

- **Round-17 Copilot review fixes + PAT rotation + Infisical sync configuration revision** (2026-04-28, 1 Copilot R17 doc-accuracy fix + ecosystem-shaping naming-convention revision + audited PAT rotation event):
  - **R17 #1 / `auto-pr-to-main.yml` step 4 trap — unconditional `rm -f "$CONTRIB_RAW"` is NOT a silent no-op** — the R14 #1 trap consolidation comment block claimed `"rm -f \"\""` was a "silent no-op until step 7's mktemp populates the variable", but on GNU coreutils (the Ubuntu-based GitHub-hosted runner) `rm -f ""` emits `rm: cannot remove '': No such file or directory` and exits 1. If the script aborts before step 7's mktemp populates `CONTRIB_RAW`, the trap's `rm -f` would alter the trap's own exit status and could mask the real script-failure exit code. Copilot R17 #1 flagged the empty-operand failure mode. **Fix**: split the trap into an unconditional cleanup for the always-populated paths + a conditional cleanup branch for the optional placeholder — `trap 'rm -f "$PR_BODY" "$PR_TITLE" "$CONTRIBUTORS_FILE"; if [ -n "$CONTRIB_RAW" ]; then rm -f "$CONTRIB_RAW"; fi' EXIT`. Comment block above the trap rewritten to (a) describe the actual GNU coreutils empty-operand behavior, correcting the R14 #1 "silent no-op" claim that was incorrect, (b) explicitly direct future contributors to add new optional temp files to the conditional branch (not the unconditional list) when their mktemp assignment happens after the trap is set. **Compliance basis**: SOC 2 CC7.2 (cleanup procedures must not alter the exit status of monitored job steps); ISO/IEC 27001:2022 A.5.37 (documented operating procedures must reflect actual runtime behavior, not assumed behavior); ISO/IEC 42001:2023 A.6.2.7 (correctness of code-comment claims is part of the AI-system documentation surface).

- **Round-16 Copilot review fixes** (2026-04-28, 4 items — 2 workflow improvements on `auto-pr-to-main.yml` + 1 ADR-004 heading rename for stable anchor + 1 CHANGELOG cross-doc link fix):
  - **R16 #1 / `auto-pr-to-main.yml` step 4 — RUNNER_TEMP fail-fast (policy-code alignment)** — the comment block above the temp-dir setup explicitly stated "$RUNNER_TEMP — the GitHub-runner-scoped temp directory that is isolated from the shared /tmp", but the code did `TEMP_DIR="${RUNNER_TEMP:-/tmp}"` — silently falling back to `/tmp` if `RUNNER_TEMP`was unset, which contradicts the documented isolation policy. Copilot R16 #1 flagged the policy-code drift. **Fix**: replaced the silent fallback with an explicit fail-fast check (`if [ -z "${RUNNER_TEMP:-}" ]; then echo "::error::..." >&2; exit 1; fi`) that aborts the workflow with a clear error message if`RUNNER_TEMP` is unset. The error message explains: GitHub-hosted runners always set `RUNNER_TEMP`; an unset value means the workflow is being executed in an unsupported environment (e.g.,`act`, local emulation without env shimming) where the isolation guarantee cannot be honored. Comments above the check were preserved unchanged because they now accurately describe the runtime behavior. **Compliance basis**: SOC 2 CC7.2 + CC8.1 (controls must enforce the documented policy, not silently degrade to a weaker stance); ISO/IEC 27001:2022 A.5.37 (documented operating procedures must be enforced by the system, not just described); ISO/IEC 42001:2023 A.6.2.7-A.6.2.8 (AI system controls must fail-closed, not fail-open).
  - **R16 #2 / `auto-pr-to-main.yml` step 7 — email-keyed cache prevents per-commit API call storm on large branches** — the contributors-list builder did one `gh api repos/.../commits/$sha` call per commit in the branch range. On a long-running branch with 50+ commits this could trigger GitHub API rate-limits, degrading attribution to the name-only fallback for the rest of the run. Copilot R16 #2 flagged the rate-limit risk. **Fix**: added a bash associative array `EMAIL_LOGIN_CACHE` keyed by author email. Before each API call, check if the email already has a memoized login (cache hit → reuse). On miss, do the API call and memoize the result (including empty-string "unresolved" outcomes so we don't retry the same email twice within one run). On a typical PR (1–5 unique authors over 5–50 commits), this reduces API calls by 50–95%. Cache is correct because GitHub's commits API resolves the same email to the same login deterministically. Cache scope is the workflow run (rebuilt fresh per `auto-pr-to-main` execution); no persistence needed. **Compliance basis**: NIST CSF 2.0 PR.IP-1 (efficient resource use); NIST CSF 2.0 PR.AC-4 (rate-limit resilience prevents control degradation under load); SOC 2 A1.2 (system processing integrity — attribution remains accurate even on large PRs).
  - **R16 #3 / `ADR-004` § Empirical Validation Results — heading rename for stable anchor** — the section heading was `## Empirical Validation Results (round-7, 2026-04-27)`. GitHub auto-generates Markdown anchors via lowercase + space-to-hyphen + non-alphanumeric drop, producing `#empirical-validation-results-round-7-2026-04-27` (with the parenthetical date suffix baked into the anchor). Internal links written as `[Empirical Validation Results](#empirical-validation-results)` (the human-intuition-friendly form) DO NOT resolve. Copilot R16 #3 flagged the broken intra-doc anchor. **Fix**: renamed the heading to `## Empirical Validation Results` (clean anchor `#empirical-validation-results`) and moved the round/date provenance to a `*Source: round-7 controlled experiment, 2026-04-27 (sharpened in round-13 #1, 2026-04-28).*` italic sub-line right under the heading. Audit trail of when the section was added + revised is preserved without polluting the anchor. **Lesson**: section headings that need to be cross-referenced should NEVER include parenthetical dates / round-numbers / version qualifiers — those go in a sub-line. Codified as a NEW operational rule (see Meta below).
  - **R16 #4 / `CHANGELOG.md` line 66 — broken intra-CHANGELOG anchor for cross-doc reference** — the R15 #1 narrative said `Cross-references [Empirical Validation Results](#empirical-validation-results) from the procedure intro`, but `#empirical-validation-results` resolves WITHIN THE CHANGELOG (where there is no such heading), not in ADR-004 where the section actually lives. Copilot R16 #4 flagged the implicit-anchor failure mode (Markdown `(#anchor)` always resolves within the current doc, not the intended target doc). **Fix**: rewritten as an explicit cross-doc link `(.github/ADR-004-copilot-auto-review-ruleset.md#empirical-validation-results)` per Copilot's suggestion. Combined with the R16 #3 heading rename, the link now resolves correctly from the CHANGELOG to the renamed clean-anchor section in ADR-004. **Lesson**: when a CHANGELOG narrative paraphrases or summarizes a cross-doc link, ALWAYS render the link as an explicit relative path (`(./relative/path.md#anchor)`), never as a bare `(#anchor)`. The latter form silently resolves to a current-doc anchor that doesn't exist. Codified as a NEW operational rule (see Meta below).

- **Round-15 Copilot review fixes** (2026-04-28, 3 items — 1 ADR-004 verification-procedure alignment + 2 workflow improvements on `auto-pr-to-main.yml`):
  - **R15 #1 / `ADR-004` § End-to-end auto-trigger validation — procedure conflicted with Empirical Validation Results** — the validation steps instructed "the next bot-authored push to any open PR is the live test" + "push a commit authored via `weown-bot` (i.e., the auto-PR workflow runs and updates / creates a PR)", but the same ADR's § Empirical Validation Results (added in R7, sharpened in R13 #1) documents that Copilot auto-review eligibility is evaluated **at PR-creation / reopen time**, NOT at push time. Running the procedure against a pre-existing PR (like PR #13 itself) produces a false negative because the PR-creation-time cache was set BEFORE ruleset enablement. Copilot R15 #1 flagged the internal contradiction. **Fix**: rewritten to explicitly require ONE of two trigger paths: **(a) New PR path** — push `weown-bot`-authored commits to a fresh branch so `auto-pr-to-main.yml` opens a brand-new PR, OR **(b) Close+reopen path** — `gh pr close <N>` then `gh pr reopen <N>` on an existing PR (Copilot re-evaluates eligibility on the reopen event). Added bold **Important** callout at top of section + explicit warning in step 4 ("Do not retry against a pre-existing PR via plain push — that path is known-unreliable per § Empirical Validation Results"). Cross-references [Empirical Validation Results](.github/ADR-004-copilot-auto-review-ruleset.md#empirical-validation-results) from the procedure intro. **Compliance basis**: SOC 2 CC7.2 (validation procedures must be executable end-to-end without producing false negatives); ISO/IEC 27001:2022 A.5.37 (documented operating procedures must correctly describe the system behavior, including timing-sensitive enforcement semantics); ISO/IEC 42001:2023 A.6.2.7-A.6.2.8 (AI validation procedures must account for the population they apply to — here, newly-created PRs only).
  - **R15 #2 / `auto-pr-to-main.yml` step 6 — two API calls collapsed into one with jq fallback chain** — the `OPENED_BY` resolution did two `gh api repos/$REPO/commits/$FIRST_SHA` calls on the same SHA: first with `--jq '.author.login // ""'`, then on empty fall through, with `--jq '.committer.login // ""'`. Both calls hit the same endpoint with the same SHA, wasting one API request per PR build + doubling rate-limit exposure. Copilot R15 #2 suggested consolidating via jq's `//` operator. **Fix**: rewritten to a single call `gh api "repos/$GITHUB_REPOSITORY/commits/$FIRST_SHA" --jq '.author.login // .committer.login // ""'` — jq evaluates the fallback chain in-process, returning the first truthy value (or empty string on total miss). Halves per-PR API requests, reducing rate-limit exposure especially under burst conditions (e.g., multiple pushes within the `concurrency:` cancel-in-progress window). Added comment block explaining the consolidation rationale + rate-limit justification. **Compliance basis**: operational cost reduction + defense-in-depth against API rate-limits (NIST CSF 2.0 PR.IP-1 — configuration baselines include efficient resource use).
  - **R15 #3 / `auto-pr-to-main.yml` step 8 — `@unknown` sentinel leakage in PR body** — lines `echo "**Opened by:** @${OPENED_BY}"` + `echo "**Last pushed by:** @${LAST_PUSHED_BY_RESOLVED}"` always prefixed `@` even when the resolved value was the fallback sentinel `unknown` (e.g., when `LAST_PUSHED_BY` env var was empty AND the API fallback chain exhausted). Output would read `@unknown` which is misleading — `@unknown` is not a valid GitHub login and looks like a broken mention. Copilot R15 #3 flagged the UX degradation. **Fix**: added `render_handle()` helper function before the step 8 `{` block that uses a bash `case` to conditionally prefix `@` only when the value is non-empty AND not the literal string `unknown`; otherwise renders plain `unknown`. Call sites changed from `@${OPENED_BY}` / `@${LAST_PUSHED_BY_RESOLVED}` to `$(render_handle "$OPENED_BY")` / `$(render_handle "$LAST_PUSHED_BY_RESOLVED")`. Output under degraded resolution now reads `**Opened by:** unknown` (no broken mention) instead of `**Opened by:** @unknown`. Helper is reusable for any future handle-rendering needs in the same workflow. **Compliance basis**: SOC 2 CC8.1 (user-facing output must accurately reflect system state); ISO/IEC 42001:2023 A.6.2.7 (AI-adjacent outputs must not produce misleading artifacts).

- **Round-14 Copilot review fixes** (2026-04-28, 6 items — 1 workflow refactor + 1 ADR-004 control-evidence wording + 4 anchor-link sweeps after R13 § header date-range change):
  - **R14 #1 / `auto-pr-to-main.yml` step 4 + step 7 trap consolidation** — the workflow set `trap '...' EXIT` twice in the same shell (line 133 covering 3 temp files, line 213 overwriting it to cover 4). Functionally correct (each trap covered exactly the files extant at that point) but Copilot flagged the override pattern as regression-risky: any future temp file added between steps would silently leak if the dev forgot to update both trap calls. **Fix**: declared `CONTRIB_RAW=""` placeholder in step 4 alongside the other mktemp calls; the single trap on line 141 now references all 4 paths upfront. The trap body single-quotes `$CONTRIB_RAW`, so variable expansion happens at fire time — empty string expands to a silent `rm -f ""` no-op until step 7 mktemp populates the variable. Step 7 retains only the `CONTRIB_RAW="$(mktemp ...)"` assignment; the duplicate trap call is removed. Added in-line comment block explaining the deliberate single-trap-with-deferred-mktemp pattern + regression-safety rationale (any future temp file just adds itself to the trap body once, with empty placeholder upfront). **Compliance basis**: SOC 2 CC8.1 (controls must be unambiguous to engineers) + ISO/IEC 27001:2022 A.5.37 (documented operating procedures must accurately describe enforcement mechanism) + WeOwn `mktemp + trap` defense-in-depth pattern from § 6 of `.github/workflows/README.md`.
  - **R14 #2 / ADR-004 § "Defense-in-depth" bullet — inaccurate control-evidence claim** — the bullet read "Ruleset `non_fast_forward` AND `auto-pr-to-main.yml`'s commit-signing requirement" but `auto-pr-to-main.yml` does NOT enforce commit signing — that's the `main` ruleset (per ADR-003 § 8.1 row 5: `required_signatures: true`). The auto-PR workflow signs ITS OWN commits (the bot-authored one creating the PR) but doesn't gate human commits on the branch. Copilot R14 #2 flagged the example as inaccurate control-evidence. **Fix**: rewritten to "Ruleset `non_fast_forward` AND ADR-003 / `main` ruleset signed-commit enforcement" — attributes the signing-enforcement to the actual control mechanism (the `main` branch ruleset) instead of the workflow. The defense-in-depth point still holds: `non_fast_forward` (Layer 1, `~ALL` branches) + `required_signatures` (`main` ruleset only) are independent enforcement layers. **Compliance basis**: SOC 2 CC7.2 (audit-trail accuracy: control-evidence statements must point to the actual enforcement mechanism, not an implementer that doesn't enforce); ISO/IEC 42001:2023 A.6.2.7-A.6.2.8 (AI control attribution must be precise to support compliance audit).
  - **R14 #3-#6 / `CHANGELOG.md` 4 broken intra-doc anchor links** — R13 #5 changed the `[v3.3.5.1]` section header from `— 2026-04-27` to `— 2026-04-27 to 2026-04-28`. GitHub auto-generates Markdown anchors by lowercasing + replacing spaces with hyphens + dropping non-alphanumerics; the new anchor is `#v3351--2026-04-27-to-2026-04-28`, breaking the previous `#v3351--2026-04-27` references. Copilot R14 #3-#6 flagged 4 occurrences across the `[v3.3.4.2]` section: (3) Changed bullet about workflows/README.md header version bump cross-reference; (4) Changed bullet about Continuation rounds 4–7 cross-reference; (5) Added bullet about Platform-sourced developer attribution three-tier upgrade cross-reference; (6) Meta bullet about Version cadence cross-reference. **Fix**: single `sed`-equivalent sweep — all 4 occurrences updated from `(#v3351--2026-04-27)` to `(#v3351--2026-04-27-to-2026-04-28)` via `replace_all` on the exact pattern. Verified zero false positives (no other `#v3351--2026-04-27` substring exists). **Lesson**: when a `## [vN.N.N.N]` section header is modified mid-PR (e.g., date-range expansion), sweep ALL intra-doc links in the same commit — GitHub-generated anchors regenerate automatically and silently break references. Codified as a NEW operational rule in this round's close-out.

- **Round-13 Copilot review fixes** (2026-04-28, 5 items — 4 doc edits committed in this round + 1 deferred manual PR title alignment):
  - **R13 #1 / `ADR-004` line 24 — `copilot_code_review` claim qualified** — the rule description claimed "every PR (regardless of base branch) gets Copilot AI review automatically" but the same ADR's § Empirical Validation Results section documents that auto-trigger is evaluated at PR-creation time and does NOT apply to pre-existing PRs (e.g., PR #13). Copilot flagged the internal contradiction. **Fix**: rewritten to "every **newly-created** PR (regardless of base branch) gets Copilot AI review automatically **after ruleset enablement**" + added explicit Note paragraph: "Copilot evaluates auto-review eligibility at PR-creation time, so PRs that already existed before the ruleset was applied (e.g., PR #13) do **not** retroactively gain auto-review — they must be triggered manually for the duration of their open lifecycle. See § Empirical Validation Results below for the controlled experiment confirming this PR-creation-time caching behavior." Also bumped Date header to add `2026-04-28 (R13 clarification on auto-trigger timing)` for audit traceability. Resolves the contradiction without softening the forward-looking claim (all NEW PRs auto-trigger; only PR #13 is grandfathered to manual-only for its remaining lifecycle).
  - **R13 #2 / `INCIDENT_RESPONSE.md` Scenario 6 step 2 — awkward "per ... per ..." phrasing rephrased** — the line read "Assign new primary steward ... per CODEOWNERS `.github/CODEOWNERS` per-path TODO comments (decision pending 2026-05-15 handoff)" which had repeated "per" and ambiguous parsing during a live incident. **Fix**: rewritten per Copilot's suggestion to "based on `.github/CODEOWNERS`; use the handoff TODO comments there to resolve the pending 2026-05-15 decision" — single canonical reference to `.github/CODEOWNERS`, semicolon-separated decision-procedure clarification, no duplicate "per" keywords. Also bumped header `Version` from `v3.3.4.1` to `v3.3.5.1` and `Date` to `2026-04-23 (initial) / 2026-04-28 (R13 phrasing fix on Scenario 6 step 2)` so the header reflects the actual last-edit state and matches the rest of the v3.3.5.1 PR scope. **Compliance basis**: incident-response runbooks must be unambiguous under stress (NIST CSF 2.0 RS.MA-1, ISO/IEC 27001:2022 A.5.24); awkward phrasing is a documented incident-response failure mode.
  - **R13 #3 / PR title vs. CHANGELOG version mismatch — deferred to manual `gh pr edit` after this commit lands** — PR #13 title was set at PR-creation time (2026-04-23) to `Auto-PR: docs(v3.3.4.2): sync main ruleset docs + ADR-003 + confirmed contributor handles`. The auto-PR workflow intentionally preserves PR titles across pushes (see R11 fix in `workflows/README.md` §2A step 9 + ADR-001), so the title naturally drifts from the effective in-flight version (`v3.3.5.1`). Copilot R13 #3 flagged the divergence between the PR title and the `[v3.3.5.1]` CHANGELOG section. **Fix**: cannot be applied via a regular commit (workflow ignores `--title` on existing PRs by design). **Action plan**: after this commit lands, manually run `gh pr edit 13 --title "docs(v3.3.5.1): main ruleset docs + ADR-003/004 + auto-PR workflow attribution + Copilot review hardening"` to align the PR title with the current effective version. This is a one-time manual override of the workflow's title-preservation contract, justified by audit-trail clarity at squash-merge time. The workflow's title-preservation rule remains correct for normal use; this is the documented exception when the in-flight version bumps mid-PR.
  - **R13 #4 / `CONTRIBUTING.md` header `Last updated: 2026-04-27` — stale date refreshed** — the file was modified in R12 (2026-04-28) but the header date wasn't updated. Copilot R13 #4 suggested bumping both Version (v3.3.5.1 → v3.3.5.2) and Last updated (2026-04-27 → 2026-04-28). **Fix applied**: `Last updated: 2026-04-28 (R12 §4 attribution-fallback fix + R13 header date sync)`. **Fix REJECTED**: keeping `Version: v3.3.5.1` rather than bumping to `v3.3.5.2`. **Rationale for rejection**: per the explicit decision in this `[v3.3.5.1]` section's intro paragraph ("Version stays at `v3.3.5.1` across the day boundary because the PR is a single semantic unit (no fresh iteration started post-merge)"), the in-flight PR keeps the same #WeOwnVer iteration number throughout. Bumping to v3.3.5.2 mid-PR would create cascade churn (CONTRIBUTING.md, workflows/README.md, ADR-003, ADR-004, CHANGELOG section header all need to match) without changing the PR's semantic scope. Documenting the rejection in the `Last updated` parenthetical communicates intent to future auditors. (A future v3.3.5.2 will be the FIRST PR opened on 2026-04-28 or later — a fresh iteration boundary.)
  - **R13 #5 / `CHANGELOG.md` `[v3.3.5.1]` header date + intro paragraph — stale single-day date refreshed to span** — the section header read `## [v3.3.5.1] — 2026-04-27` and the intro claimed "all changes in this section were made on 2026-04-27", but R12 added a 2026-04-28-dated entry. Copilot R13 #5 flagged the inconsistency. **Fix**: header rewritten to `## [v3.3.5.1] — 2026-04-27 to 2026-04-28` per Copilot's suggested rewording; intro paragraph rewritten to "this section captures the portion of the PR that moved into the 5th week of April under the active #WeOwnVer interpretation ... the later continuation rounds documented here span 2026-04-27 (rounds 4–11) through 2026-04-28 (rounds 12–13)" + added explicit "Version stays at `v3.3.5.1` across the day boundary because the PR is a single semantic unit (no fresh iteration started post-merge)" sentence so the v3.3.5.1-not-v3.3.5.2 decision is auditable from the section header alone (cross-references R13 #4 above).

- **Round-12 Copilot review fixes** (2026-04-28, 1 item — in `CONTRIBUTING.md` §4 attribution paragraph):
  - **R12 #1 / §4 post-attribution closing sentence** — the trailing sentence claimed "The PR body **always** shows real GitHub usernames (`@ncimino`, `@romandidomizio`, etc.), regardless of what `<dev>` segment was chosen" which contradicted the documented step-7 aggregation fallback behavior (per `workflows/README.md` §2A step 7 + `auto-pr-to-main.yml` step 7: `gh api` returns `.author.login // .committer.login // ""`; on empty, falls through to `git log -1 --format=%an` producing a plain-text author name, no `@` prefix, no GitHub link). Copilot flagged "always" as overly absolute. **Fix**: rewritten to "shows real GitHub usernames (`@ncimino`, `@romandidomizio`, etc.) **when available** and otherwise falls back to commit-author names (for commits where the commits API doesn't return a linked GitHub login — e.g., unlinked email addresses), regardless of what `<dev>` segment was chosen for the branch name." Matches Copilot's suggested rewording and adds a parenthetical explaining the specific failure mode (unlinked email) so maintainers understand why the fallback exists. **Compliance alignment**: SOC 2 CC8.1 + ISO/IEC 27001:2022 A.5.37 (documented operating procedures must be accurate) — the "always" claim would fail an audit where the auditor traces the code path and finds the fallback branch.

- **Round-11 Copilot review fixes** (2026-04-27, 1 item — in `workflows/README.md` §2A walkthrough table):
  - **R11 #1 / §2A step 9 row** — the "What it does" column claimed the existing-PR idempotent-update path runs `gh pr edit $N --title --body --add-reviewer ncimino,romandidomizio` (single combined call, updates title). In reality, `auto-pr-to-main.yml` lines 353–357 intentionally **preserve the existing PR title** and make **two separate `gh pr edit` calls**: `gh pr edit $N --body-file $PR_BODY` (body only — no `--title` flag passed), followed by `gh pr edit $N --add-reviewer ncimino,romandidomizio` (reviewers only). Title is set exactly once at PR creation time from `$PR_TITLE` (step 5). Row rewritten to accurately reflect the implementation: "If found → `gh pr edit $N --body-file $PR_BODY` (preserves the existing title — PR titles are set once at creation, not refreshed on subsequent pushes), followed by a separate `gh pr edit $N --add-reviewer ncimino,romandidomizio`. If not → `gh pr create --base main --head $BRANCH_NAME --title $(cat $PR_TITLE) --body-file $PR_BODY` followed by the same `--add-reviewer` call." **Rationale for title preservation** (now explicit in doc): PR titles tend to carry human semantic context (feature scope, PR tracking nicknames); refreshing them on every push based on the latest commit subject would create churn in PR lists + notifications. Body is always refreshed to reflect latest commits + attribution. This matches GitHub best-practice for long-lived PRs with iterative review rounds (exactly the scenario PR #13 is in — title stable for 11 rounds, body refreshed each push). Compliance alignment: SOC 2 CC8.1 change-management — accurate developer documentation is part of the audit-trail evidence chain (the walkthrough table IS the onboarding doc for new contributors); inaccurate documentation of a security-sensitive workflow step would be flagged in an audit.

- **Round-10 Copilot review fixes** (2026-04-27, 2 items — both in this `CHANGELOG.md`):
  - **R10 #1 / `### Fixed` round-8 close-out entry** + **R10 #2 / `### Security / Compliance` PII audit bullet** — the round-8 entry quoted the literal personal email 2x ('Replace temp `<actual-email>` with permanent bot email' + 'stale `<actual-email>` references in §2 + §7') as part of describing the round-8 fix, and the PII-audit bullet quoted it 1x ('all 3 `<actual-email>` references in `workflows/README.md` rewritten...'). All 3 occurrences flagged by Copilot R10 as still exposing PII despite the fix being correctly described. **Fix**: all 3 occurrences replaced with `<redacted-personal-email>` placeholder — same convention used elsewhere in the codebase for placeholder identifiers (e.g., `<dev>`, `<real-github-username>` in v3.3.4.2 entry). Rationale and round-8 audit history fully preserved; only the literal email string is gone. **Anticipated in round-8 close-out commit message**: that commit explicitly noted "the email appears 3x in the round-8 commit body documenting Before: state of the fix" and identified squash-merge as the long-term sanitization mechanism. Round-10 brings forward that sanitization to the CHANGELOG narrative now (rather than waiting for squash-merge) so the in-flight CHANGELOG no longer carries the email. Same redaction approach should be applied to the round-10 close-out commit message itself — i.e., this entry must NOT quote the literal email it just redacted.

- **Round-9 Copilot review fixes** (2026-04-27, 3 items + 1 plain-text-handle defense-in-depth extension — all in `workflows/README.md`):
  - **R9 #1 / §2 Account Security Requirements line 64** — "✅ 2FA mandatory (TOTP + recovery codes held by Yonks as enterprise admin)" disclosed specific 2FA credential custody on a public repo. Round-3 had already removed this pattern from §10 Transition Checklist row 2 ("Transfer 2FA (TOTP seed) + recovery codes..." → "Transfer 2FA administration per internal runbook...") but missed the §2 line. **Fix**: rewritten to "✅ 2FA mandatory (administration, recovery, and custody tracked per internal runbook — no credential details in this public repo)". Aligns with the round-3/7/8 internal-runbook posture. Reduces social-engineering surface area (no specific credential types named, no custodian named in this section). Compliance basis: ISO/IEC 27001:2022 A.5.34 (PII protection) + A.8.1 (user endpoint devices) + NIST CSF 2.0 PR.AC-3 (access management without revealing custody chain).
  - **R9 #2 / §11 Troubleshooting Copilot auto-review row** — manual-trigger snippet used `reviewers[]=Copilot` but ADR-004 § Validation (line 169) references the canonical login `copilot-pull-request-reviewer`. Inconsistency could cause API call to fail (`Copilot` is not a valid GitHub login) or confuse maintainers cross-referencing the two docs. **Fix**: snippet rewritten to use `reviewers[]=copilot-pull-request-reviewer` with explicit "canonical GitHub Copilot reviewer login — same value referenced by ADR-004 § Validation" cross-reference. Single canonical identifier across the docs reduces divergence risk.
  - **R9 #3 / §10 Transition Checklist rows 3 + 6** — Owner column had plain-text "Yonks" (row 3 sole owner; row 6 second owner) while adjacent rows used `@YonksTEAM` handle. Inconsistency makes unambiguous paging during transition impossible (audit trail can't `@`-mention plain-text names). **Fix**: both rows updated to `@YonksTEAM` handle. All 6 rows in the Transition Checklist now use GitHub handles consistently for the Owner column.
  - **PII / handle-consistency defense-in-depth note**: a separate sweep found that `SECURITY_ASSESSMENT.md` line 14 ("Yonks-held recovery codes") and `INCIDENT_RESPONSE.md` lines 61, 149, 152 (similar credential-custody disclosures + plain-text "Yonks" handles) carry the same anti-patterns. These files are OUT OF SCOPE for PR #13 (which is the main-branch-ruleset + auto-PR + Copilot-review docs sweep) and intentionally NOT modified to avoid scope creep. **Tracked**: a follow-up PR (v3.3.5.2 or v3.3.6.1) will apply the same internal-runbook + handle-consistency rewrites across `SECURITY_ASSESSMENT.md` + `INCIDENT_RESPONSE.md`. Documented in `PR7_HANDOFF_CHECKLIST.md` queue.

- **Round-8 Copilot review fixes** (2026-04-27, 3 items — all in `workflows/README.md`):
  - **R8 #1 / §10 Transition Checklist row 3** — "Replace temp `<redacted-personal-email>` with permanent bot email" exposed a real personal email on a public repo. **Fix**: rewritten to "Update the service account's email to the permanent bot email (details tracked per internal runbook)". Defense-in-depth extension (same principle): stale `<redacted-personal-email>` references in §2 Account Security Requirements (line 65) and §7 Layer 1 email alert recipient (line 390) also rewritten to generic internal-runbook language to prevent Copilot from flagging them in a future round and to complete the PII audit. Aligns with the round-6/7 PII-minimization posture already applied to `CONTRIBUTING.md` handle table + `CODEOWNERS` + step 7 contributors fallback + step 8 Recent Commits. Personal emails are PII per GDPR Art. 5(1)(c) data minimization + ISO/IEC 27001:2022 A.5.34 even when the individual is publicly associated with the repo.
  - **R8 #2 / §11 Troubleshooting 403 row** — the suggested PAT scopes (`contents:write, pull_requests:write, issues:write, metadata:read`) contradicted the least-privilege guidance at §2.4 + ADR-001 (`Contents: Read, Pull requests: Read/Write, Metadata: Read`; `Issues: Write` intentionally NOT on the PAT). **Fix**: row rewritten to specify the 3 correct least-privilege scopes, explicit "Do NOT add `Issues: Write` to the PAT" warning, cross-reference to §2.4 + ADR-001, and explanation that `permissions: issues: write` on the workflow job (using the ephemeral `GITHUB_TOKEN`) is the correct mechanism for issue actions — not a broader PAT scope. Prevents a future maintainer following the troubleshooting table from over-provisioning the PAT against the repo's own security posture.
  - **R8 #3 / §2A step 6 failure mode** — column said `gh api` rate-limited "uses `$GITHUB_TOKEN`", but `auto-pr-to-main.yml` actually runs every `gh` call under `GH_TOKEN=${{ secrets.WEOWN_BOT_PAT }}` (the PAT is what makes PRs show `weown-bot` as author, which is what makes Copilot auto-review trigger — see inline comment in the workflow). **Fix**: failure mode now reads "check `WEOWN_BOT_PAT` rate-limit status / scopes / rotation state (the workflow runs all `gh` calls under `GH_TOKEN=${{ secrets.WEOWN_BOT_PAT }}`, not the ephemeral `GITHUB_TOKEN`)". Correct diagnostic reference for on-call debugging.

### Security / Compliance

- **Copilot Business entitlement validated end-to-end on new PRs (2026-04-27)** — controlled experiment confirmed the Layer 2 enterprise-level ruleset triggers `Copilot AI review requested due to automatic review settings` for newly-created `weown-bot`-authored PRs without manual intervention. PR #13 stays manual-review-only (PR-creation-time caching). All future bot-authored PRs (v3.3.5.2+ work) will auto-trigger. Compliance impact: **ISO/IEC 42001:2023 A.6.2.7 (AI-aware technical controls) + A.6.2.8 (review of AI-generated output)** are now actively enforced for all NEW bot-authored PRs in addition to human-authored ones. SOC 2 CC8.1 change-management evidence narrative now has a verified AI-assisted review layer. CIS Controls v8 18.3 (development security — code review) coverage upgraded from "manual on bot PRs" to "automatic for new PRs / manual for legacy".
- **PII minimization audit (round-6 + round-7 + round-8 + round-9)**: zero email addresses anywhere in PR body output paths. Step 7 contributors fallback (`%an` only), step 8 Recent Commits header (`%an` only). Round-8 extension: all 3 `<redacted-personal-email>` references in `workflows/README.md` rewritten to generic internal-runbook language. **Round-9 extension**: §2 Account Security Requirements line 64 rewritten to remove "TOTP + recovery codes held by Yonks as enterprise admin" credential-custody disclosure (last social-engineering-surface item in workflows/README.md §2 — the §10 row 2 equivalent was already fixed in round-3). Aligns with prior PII minimization on `CONTRIBUTING.md` handle table + `CODEOWNERS`. Per GDPR Art. 5(1)(c) data minimization, ISO/IEC 27001:2022 A.5.34 (privacy + protection of PII), and ISO/IEC 27001:2022 A.5.7 (threat intelligence — reduces public attacker recon surface). (Out-of-scope files NOT modified: `vaultwarden/README.md`, `vaultwarden/CHANGELOG.md`, `anythingllm/helm/Chart.yaml` for emails; `SECURITY_ASSESSMENT.md` line 14 + `INCIDENT_RESPONSE.md` lines 61/149/152 for credential-custody + plain-text handles — follow-up PR queued.)
- **Defense-in-depth ruleset architecture** documented in ADR-004 — see "Added" entry above. SOC 2 auditor narrative: WeOwnNetwork/ai now has THREE rulesets (`main rules` id 12119304 strict on `~DEFAULT_BRANCH`, `Copilot auto-review` id 12131972 baseline on `~ALL` repo-scoped, plus enterprise-level layer baseline on all orgs/repos/branches). Bypass list empty on all three.

### Meta

- **Version cadence (revised under user's #WeOwnVer week-5 interpretation)**: 2026-04-23 work was Season 3, April Week 4 → `v3.3.4.x` (original PR + rounds 1–3). 2026-04-27 work is Season 3, April Week 5 → `v3.3.5.x` (rounds 4–7). The exact ISO-week-to-WEEK mapping methodology is currently under leadership review per `docs/VERSIONING_WEOWNVER.md`; this CHANGELOG follows the user's directive at PR #13 round-7 close-out time. Future iterations on 2026-04-27 itself would be `v3.3.5.2`, `v3.3.5.3`, etc.
- **Round 7 close-out scope** = 8 Copilot R7 doc/PII/bug fixes + 2 user-directed README additions (§2A + §11) + new ADR-004 Empirical Validation Results section + version bump cascade across 4 files + CHANGELOG split. Single commit; squash-merge target preserved.
- **Round 8 close-out scope** = 3 Copilot R8 doc-accuracy fixes on `workflows/README.md` (PII, least-privilege PAT scopes, correct `GH_TOKEN` diagnostic reference) + 2 PII defense-in-depth extensions (same stale email in §2 + §7 rewritten before Copilot flags them). Single-file change; single commit; squash-merge target preserved. Running total: 38 Copilot comments resolved across 8 rounds.
- **Round 9 close-out scope** = 3 Copilot R9 fixes on `workflows/README.md` (credential-custody PII removal in §2, canonical Copilot reviewer login `copilot-pull-request-reviewer` aligned across docs in §11, plain-text "Yonks" → `@YonksTEAM` handle in §10 rows 3 + 6 for unambiguous paging). Single-file change; single commit; squash-merge target preserved. Running total: 41 Copilot comments resolved across 9 rounds. Out-of-scope follow-ups queued: cross-doc PII sweep on `SECURITY_ASSESSMENT.md` + `INCIDENT_RESPONSE.md` for the same credential-custody anti-pattern.
- **Round 10 close-out scope** = 2 Copilot R10 fixes on `CHANGELOG.md` itself (PII self-redaction — the round-8 close-out narrative + PII audit bullet quoted the literal personal email 3x while documenting the round-8 fix; replaced with `<redacted-personal-email>` placeholder, full audit rationale preserved). Single-file change; single commit; squash-merge target preserved. Running total: 43 Copilot comments resolved across 10 rounds. **Lesson learned**: when documenting a PII fix, the rationale entry itself must use a placeholder — quoting the original 'Before:' value defeats the purpose of the fix. This applies recursively to the round-10 commit message + this CHANGELOG entry.
- **Round 11 close-out scope** = 1 Copilot R11 doc-accuracy fix on `workflows/README.md` §2A walkthrough table step 9 (documented `--title --body --add-reviewer` single combined call → rewritten to match actual implementation of two separate `gh pr edit` calls with title preservation). Single-file change; single commit; squash-merge target preserved. Running total: 44 Copilot comments resolved across 11 rounds. **Pattern**: late-round reviews are surfacing walkthrough-table accuracy drift that earlier rounds missed — `§2A` is now the most heavily-reviewed section of the doc.
- **Round 12 close-out scope** = 1 Copilot R12 doc-accuracy fix on `CONTRIBUTING.md` §4 attribution paragraph (removed overly-absolute "always" claim; added fallback-to-name path that matches step-7 implementation). Single-file change; single commit; squash-merge target preserved. Running total: 45 Copilot comments resolved across 12 rounds. **Pattern continuation**: accuracy-drift fixes now extending beyond `workflows/README.md` §2A into adjacent docs (`CONTRIBUTING.md` §4) that describe the same workflow behavior. Going-forward rule: when the attribution / step-7 code path changes, sweep BOTH `workflows/README.md` §2A step 7 + `CONTRIBUTING.md` §4 for consistency in the same commit.
- **Round 13 close-out scope** = 5 Copilot R13 fixes touching 4 files (`ADR-004` auto-trigger qualification + Date bump; `INCIDENT_RESPONSE.md` Scenario 6 phrasing + version/date header sync; `CONTRIBUTING.md` Last-updated date sync; `CHANGELOG.md` `[v3.3.5.1]` section header + intro paragraph spanning 2026-04-27→-28) + 1 deferred manual `gh pr edit 13 --title` override (R13 #3). Multi-file change; single commit; squash-merge target preserved. Running total: 50 Copilot comments resolved across 13 rounds. **Pattern continuation**: accuracy-drift fixes are now sweeping ALL versioned-doc headers in the PR scope simultaneously (ADR-004, INCIDENT_RESPONSE.md, CONTRIBUTING.md, CHANGELOG section header) rather than waiting for Copilot to flag each one. **Going-forward rule**: when crossing the day boundary mid-PR, refresh `Last updated` on every modified file in the same commit AND update the `[vN.N.N.N]` CHANGELOG section header to span the date range. **R13 § Empirical Validation Results consistency rule**: when a forward-looking compliance claim (e.g., "every PR gets X automatically") is documented in an ADR, ensure ALL such claims explicitly reference the validation section that confirms or qualifies the claim — prevents internal contradictions like the one R13 #1 caught.
- **Round 14 close-out scope** = 6 Copilot R14 fixes touching 3 files (`auto-pr-to-main.yml` trap consolidation; `ADR-004` defense-in-depth bullet control-evidence wording; `CHANGELOG.md` 4 anchor-link sweeps). Multi-file change; single commit; squash-merge target preserved. Running total: 56 Copilot comments resolved across 14 rounds. **Pattern continuation**: R14 surfaced a NEW class of accuracy drift (intra-doc anchor breakage caused by R13 § header date-range change), confirming that consistency sweeps must extend to all link-target derivations, not just text content. **Three NEW operational rules codified**: (a) **Same-shell trap discipline** — within a single `run:` block (one bash shell), use a SINGLE `trap '...' EXIT` set ONCE upfront, with all temp-file paths declared as variables (empty placeholder OK; bash expands at fire time). NEVER re-`trap` in the same shell to add a new file; just initialize the variable to `""` upfront and assign mktemp later. Override patterns are regression-risky and Copilot-flag-prone. (b) **Post-anchor-change link sweep** — when modifying a `## [vN.N.N.N]` section header in CHANGELOG (or any Markdown header that other parts of the same doc cross-reference), sweep ALL intra-doc `(#anchor)` references in the same commit. GitHub auto-generates anchors from header text via lowercasing + space-to-hyphen + non-alphanumeric drop; date-range expansions, version bumps, and any other header edit silently break links. (c) **ADR control-evidence accuracy** — when an ADR cites a control-evidence example (e.g., "X is enforced by mechanism Y"), verify Y is the ACTUAL enforcement mechanism (not just an implementer that interacts with Y). Cross-reference the relevant ADR's § "Rules enabled" or § "Configuration" section to confirm the control-evidence path is end-to-end accurate.
- **Round 15 close-out scope** = 3 Copilot R15 fixes touching 2 files (`ADR-004` validation procedure alignment with Empirical Validation Results; `auto-pr-to-main.yml` two code-quality improvements — API call consolidation via jq fallback chain + `render_handle` helper preventing `@unknown` sentinel leakage). Multi-file change; single commit; squash-merge target preserved. Running total: 59 Copilot comments resolved across 15 rounds. **Pattern continuation**: R15 surfaced a NEW class of accuracy drift (validation-procedure semantic conflict with the same ADR's own empirical findings), confirming that consistency sweeps must extend to verification / troubleshooting / runbook sections of an ADR, not just the main body. **ONE NEW operational rule codified**: (a) **Sentinel-value output discipline** — when emitting user-visible strings that may include fallback sentinel values (e.g., `unknown`, `n/a`, `unresolved`), NEVER prefix them with formatting that implies a valid reference (e.g., `@unknown` implies a GitHub mention, `#unknown` implies an issue/PR). Use a small render helper function with a `case` to conditionally apply the prefix only for real values, and render a plain sentinel otherwise. This pattern applies to GitHub handles, issue/PR numbers, commit SHAs, email addresses, and any other bracket/sigil-prefixed identifier. **R15 also sharpened the ADR validation cross-reference rule (R13)**: whenever a validation / debug / runbook procedure is written IN an ADR, cross-reference the ADR's own § Empirical Validation Results from the procedure intro — prevents the procedure from drifting out of sync with empirical findings when the latter are updated. This is an extension of the R13 rule (which covered main-body compliance claims) to cover procedural / instructional content as well.
- **Round 16 close-out scope** = 4 Copilot R16 fixes touching 3 files (`auto-pr-to-main.yml` two improvements: RUNNER_TEMP fail-fast for policy-code alignment + email-keyed cache for step 7 contributors-list builder; `ADR-004` heading rename for stable anchor; `CHANGELOG.md` cross-doc link fix). Multi-file change; single commit; squash-merge target preserved. Running total: 63 Copilot comments resolved across 16 rounds.
- **Round 17 close-out scope** = 1 Copilot R17 doc-accuracy fix (`auto-pr-to-main.yml` trap conditional cleanup, correcting the R14 #1 "silent no-op" claim that was incorrect on GNU coreutils) + ecosystem-shaping ADR-002 naming-convention revision (Infisical Sync Key Schema is identity-only, not strip-capable; revised to project-per-scope + identity-mapped secret name) + audited PAT rotation event (regenerated 2026-04-28; 90-day expiration 2026-07-27; stored in Infisical under the new convention). Multi-file change touching 5 files (`auto-pr-to-main.yml`, `workflows/README.md`, `pat-health-check.yml`, `ADR-002`, `CHANGELOG.md`). Single commit; squash-merge target preserved. Running total: 64 Copilot comments resolved across 17 rounds.
- **Round 18 close-out scope** = 4 Copilot R18 doc-consistency fixes (R18 #1 `workflows/README.md` §2 Scope & Principles items 3+4 rewritten to match §2.4 Usage Table; R18 #2 `INCIDENT_RESPONSE.md` Scenario 6 step 6 updated to reference per-repo folder; R18 #3 `ADR-004` Layer 1 bullet #3 qualified to align with § Empirical Validation Results PR-creation-time caching semantics + close+reopen remediation note; R18 #4 `workflows/README.md` header `Last updated` 2026-04-27 → 2026-04-28) + user-driven 2nd-iteration revision of the 2026-04-28 Infisical naming convention (project-per-repo → folder-per-repo inside the single shared `weown-bot GitHub PATs` project; namespacing axis is now folder path scoped via Sync Source Path; cleaner operational footprint — one project-level RBAC boundary instead of N). Multi-file change touching 5 files (`workflows/README.md`, `ADR-002`, `ADR-004`, `INCIDENT_RESPONSE.md`, `CHANGELOG.md`). Single commit; squash-merge target preserved. Running total: 68 Copilot comments resolved across 18 rounds. **Pattern continuation**: R18 surfaced a NEW class of drift — same-day convention iteration drift (R17 introduced a vendor-feature-corrected naming convention that itself needed a second-cut revision once the operational namespacing primitives were exercised in practice; the R17 → R18 evolution happened within the same 24-hour window). **TWO NEW operational rules codified**: (a) **Same-document consistency sweep on convention changes** — when revising a convention in section X of a doc (e.g., §2.4 Usage Table), sweep ALL earlier sections of the same doc that introduce or summarize the convention (e.g., §2 Scope & Principles, §1 inventory) in the same commit. Documents that introduce a convention before describing the table that uses it must keep the introductory text in lockstep with the table; otherwise onboarding contributors read contradictory guidance. (b) **Convention iteration discipline within the same close-out window** — when a vendor-feature-driven convention is revised under empirical sync-configuration findings (R17), expect one to two same-day operational-simplification iterations (R18) before the convention stabilizes. Document each iteration as its own Decision Log row; preserve earlier rows for audit trail. The R17 first-iteration row (project-per-repo) was preserved verbatim in ADR-002 Decision Log even though the R18 second-iteration row supersedes it operationally — this preserves the empirical narrative for future ADR review cadences. **R18 also extends the R17 rule** ("vendor-feature verification before convention design"): the verification step must include exercising the namespacing primitive against the actual UI before publication, not just confirming the primitive exists. **Pattern continuation**: R17 surfaced a NEW class of drift — vendor-feature assumption drift (an ADR encoded a convention based on an assumed vendor capability that does not exist; the assumption was unchallenged for 5 days until empirical sync-configuration on 2026-04-28). **ONE NEW operational rule codified**: (a) **Vendor-feature verification before convention design** — when an architecture or convention relies on a specific vendor feature (e.g., "Infisical Sync supports per-secret rename", "GitHub Actions exposes `triggering_actor` in `pull_request` events"), validate the feature exists in the actual UI/API before encoding it in ADR conventions. Document the verification step (UI screenshot, API response capture, or documentation excerpt) in the ADR's Implementation Notes section. Future ADR review cadences should include a "verify cited vendor features still exist" step. **R14 #1 "silent no-op" claim correction**: the R14 #1 close-out narrative claimed `"rm -f \"\""` was a silent no-op; this was incorrect on GNU coreutils. The R17 fix replaces the unconditional cleanup with a conditional branch + corrects the comment block. The R14 #1 CHANGELOG entry is preserved as historical record (the trap consolidation itself was correct; only the empty-operand sub-claim was wrong). **PAT rotation 2026-04-28 audit trail**: PAT regenerated by `@romandidomizio` from `weown-bot` account; old PAT invalidated; new PAT 90-day fine-grained, single-repo scope (`WeOwnNetwork/ai`), permissions Contents: R / PRs: R/W / Metadata; expiration 2026-07-27. Health-check verification scheduled post-Infisical-sync-completion. Resolves the 2026-04-23 sync drift permanently via the revised naming convention. **Pattern continuation**: R16 surfaced TWO NEW classes of drift: (i) policy-code drift (comments declared a stricter policy than the code enforced — R16 #1) and (ii) anchor-derivation drift (heading text containing parenthetical date suffixes silently breaks intra-doc references; cross-doc links rendered as bare `(#anchor)` silently resolve to non-existent current-doc anchors — R16 #3 + #4). **TWO NEW operational rules codified**: (a) **Stable-anchor heading discipline** — section headings that need to be cross-referenced should NEVER include parenthetical dates / round-numbers / version qualifiers / any non-alphanumeric noise; provenance metadata goes in an italic sub-line right under the heading (`*Source: ...*`) instead. Keeps the auto-generated anchor stable across edits. (b) **Cross-doc link explicitness** — when a CHANGELOG narrative (or any document) paraphrases / summarizes / references content in another file, ALWAYS render the link as an explicit relative path (`(./relative/path.md#anchor)`), never as a bare `(#anchor)`. The bare form silently resolves to a current-doc anchor that may not exist. **R16 also sharpened the policy-code alignment rule**: when a comment block declares a security / isolation / privacy policy (e.g., "isolated from /tmp", "emails are PII and not surfaced"), verify the code actually enforces it without silent fallbacks. Fail-fast on policy violations, never fall back to a weaker stance. This is the natural code-side counterpart of the R14 #2 ADR control-evidence accuracy rule (which covered ADR text claims about control mechanisms).
- **Round 19 close-out scope** = 1 Copilot R19 label-canonicalization fix (`workflows/README.md` §3 "Branch name vs. PR body" table row label `Contributors:` → canonical `Contributors on this branch:` to match the actual workflow output at `auto-pr-to-main.yml` step 8 line 320 + the same doc's earlier §3 three-tier table at line 161) + 3 same-doc + cross-doc cascades caught proactively per the **R18 #1 "Same-Document Consistency Sweep" rule** codified in the previous round (workflows/README.md §11 Troubleshooting row; CONTRIBUTING.md §4 explanatory bullet at line 334; CONTRIBUTING.md §4 parenthetical at line 328) + CONTRIBUTING.md `Last updated` header bump for R19 traceability. CHANGELOG `[v3.3.5.1]` line 56 historical narrative was explicitly PRESERVED to retain the "before/after" audit trail of the original R7 rename event (modifying the historical "before" value would corrupt the rename narrative). Multi-file change touching 3 in-repo files (`workflows/README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`) + gitignored `PR7_HANDOFF_CHECKLIST.md`. Single commit; squash-merge target preserved. Running total: 69 Copilot comments resolved across 19 rounds. **Pattern continuation**: R19 #1 is the **canonical demonstration of the R18 #1 "Same-Document Consistency Sweep" rule's value** — Copilot caught a label inconsistency at line 170 that the SAME doc had already fixed at line 161 (same §3 section, just 9 lines earlier); the R18 rule mandates exactly this kind of intra-section sweep, and R19 #1 is the kind of drift the rule was designed to surface within 1 round of the rule being codified. **TWO additional sub-rules added under the R18 rule's scope**: (a) **Workflow-output vs. doc-citation parity** — when a doc cites the label of a workflow-emitted line (e.g., a PR body field name), the citation MUST match the actual emitted string verbatim, including any modifier phrases (e.g., "on this branch"). Search-grep before publication: `grep '<label>' $WORKFLOW_FILE` to confirm the canonical form. (b) **Historical-narrative preservation in audit trails** — when a CHANGELOG entry documents a rename or label change, the original-value reference in the close-out narrative MUST be preserved verbatim even if subsequent rounds touch the same area. Modifying the quoted historical value corrupts the audit trail (R10 PII-recursive-quote lesson applies in reverse). For R19, this meant explicitly NOT touching CHANGELOG line 56 even though it contains a `Contributors:` reference, because that reference is part of the historical "before" narrative of the R7 rename event.
- **Round 20 close-out scope** = 2 Copilot R20 fixes — (1) **R20 #1 runbook-correctness fix** on `workflows/README.md` §11 Troubleshooting "Merge blocked with 'requires signed commits'" row: previous guidance said "add a new signed commit to the branch" which is incorrect (adding a new signed commit does NOT fix earlier unsigned commits — the merge will still be blocked because branch protection requires ALL commits be signed); rewritten per Copilot's literal suggested change to direct contributors to recreate the branch/PR with all commits signed since `non-fast-forward` blocks history rewriting. (2) **R20 #2 doc-vs-UI display canonicalization**: Infisical folder paths were inconsistently shown with vs. without trailing slash; user explicitly chose the **NO-trailing-slash** form (opposite of Copilot's literal suggestion to add slashes everywhere) because the Infisical UI's "Source secret path" field accepts the path without trailing slash and mirrors it verbatim, so the no-slash form survives the docs → UI → docs round-trip without transformation. Cascade across 3 docs: `workflows/README.md` (~14 path normalizations across §2, §2.4, §4.4, §5.1, §6, §6.1); `INCIDENT_RESPONSE.md` (Scenario 6 step 6); `ADR-002` (Architecture diagram + Naming Convention + Implementation Notes). ADR-002 Decision Log appended with R20 row in correct chronological position after R17 + R18 (NOT a replacement); R17 + R18 historical rows PRESERVED with their original trailing-slash forms per the R19-codified historical-narrative-preservation rule. CHANGELOG `[v3.3.5.1]` § Changed historical bullets (lines describing R17 + R18 work in their original narrative form) also PRESERVED for the same reason. ADR-002 + INCIDENT_RESPONSE.md Date headers bumped to include R20 references; `workflows/README.md` Last updated header was already simply `2026-04-28` so no change needed. Multi-file change touching 4 in-repo files (`workflows/README.md`, `ADR-002-infisical-github-sync.md`, `INCIDENT_RESPONSE.md`, `CHANGELOG.md`) + gitignored `PR7_HANDOFF_CHECKLIST.md`. Single commit; squash-merge target preserved. Running total: 71 Copilot comments resolved across 20 rounds. **Pattern continuation**: R20 surfaced TWO new drift classes — (a) **runbook-correctness drift** (R20 #1) where the narrative was internally consistent but the conclusion was wrong; survived 19 prior rounds because Copilot reviews tend to flag inconsistency rather than logical-correctness bugs in remediation steps. (b) **doc-vs-UI display drift** (R20 #2) where docs displayed paths in a form that differs from the form the vendor UI accepts/displays, creating real-world copy/paste configuration errors during incident response or new-repo onboarding. **TWO NEW operational rules codified under R20**: (1) **Runbook remediation steps must validate against the underlying enforcement chain** — when a troubleshooting row gives a remediation step, trace the step against the enforcement mechanism (here: branch-protection ruleset rule + signed-commits requirement + non_fast_forward enforcement) to verify the step actually unblocks the user. Internal narrative consistency is necessary but not sufficient. (2) **Vendor-UI parity for path-like identifiers** — when documenting a path-like identifier that users will copy into a vendor UI text field, pick the canonical form that the vendor UI itself uses (or the simpler form when the UI accepts both) and use it consistently across all docs. The cost of doc-vs-UI display mismatch is real-world copy/paste configuration errors during incident response or new-repo onboarding. **Going-forward implication**: the v3.3.5.1 review cycle (rounds 7-20, 14 rounds total) is establishing a stable taxonomy of doc-accuracy-drift classes that future PRs can pre-validate against — currently 12+ distinct sub-classes documented across all R-rounds.

---

## [v3.3.4.2] — 2026-04-23

Second #WeOwnVer iteration in the same week, following PR #7 (v3.3.4.1) merge. Ships documentation sync for the `main` branch ruleset (configured on the same day as the merge), a new ADR capturing ruleset decisions + compliance mappings, an inline contributors table + enforcement-posture matrix in `CONTRIBUTING.md` §4, and a small doc correction surfaced during workflow testing.

### Added

- **`.github/ADR-003-main-branch-ruleset.md`** — new ADR documenting the 12 rules enabled on the `main` ruleset (configured 2026-04-23), mapped to SOC 2 (CC6.1, CC6.3, CC7.1, CC7.2, CC8.1), ISO/IEC 27001:2022 (A.5.15, A.5.28, A.5.37, A.8.13, A.8.24, A.8.29, A.8.32), ISO/IEC 42001:2023 (A.6.2.7, A.6.2.8, A.7.2, A.8.3, A.9.3, A.9.4), NIST CSF 2.0 (PR.AC-4, PR.DS-6, PR.IP-1, PR.IP-3, DE.CM-1, DE.CM-7, DE.CM-8), and CIS Controls v8 (3.11, 11.2, 11.3, 16.1, 16.9, 16.11, 16.12, 18.3). Also documents rules intentionally NOT enabled (linear history, code scanning results, signed tags — latter two tracked as future items), rationale for empty bypass list under SOC 2 CC6.3, and a "Dev Attribution Enforcement Posture" section comparing three options (A=strict regex allowlist, B=reviewer-enforced convention [chosen], C=hybrid warning layer) with explicit numeric upgrade triggers (e.g., "escalate to Option C if team >15 or >10% of merged PRs use non-table handles in a quarter").
- **Platform-sourced developer attribution in `auto-pr-to-main.yml` step 6** — the workflow reads `${{ github.triggering_actor || github.actor }}` to attribute PRs, producing `**Triggered by:** @<real-github-username>` in the PR body. Zero maintenance: no case-statement mapping to keep in sync, no onboarding / offboarding workflow edits required, no risk of stale mappings drifting from `CONTRIBUTING.md`. The `<dev>` segment in branch names is preserved for human-readable naming only. Resolves 4 of 6 Copilot round-1 review comments on PR #13 that flagged `@<short-handle>` as potential misattribution. (An intermediate iteration added a case-statement mapping; subsequent iteration replaced it with `${{ github.triggering_actor || github.actor }}` for correctness-by-default. The single `Triggered by:` line was later split into a three-tier model on 2026-04-27 — see the [`[v3.3.5.1]`](#v3351--2026-04-27-to-2026-04-28) section above for the canonical record of that semantic upgrade.)

### Changed

- **`.github/workflows/README.md` §8.1** rewritten to reflect the as-configured ruleset (12 enabled rules with compliance column) instead of an aspirational recommendation list. Removes `pat-health-check.yml` from "required status checks" (it is `schedule:` triggered, cannot be a PR-time gate). Clarifies that "Require code quality results at warning and higher" is satisfied by **CodeQL Default Setup** (distinct from "Require code scanning results" which requires SARIF via a different API). Adds explicit note on why linear history is intentionally NOT enabled and how "Restrict who can push" is effectively covered by "Require a PR" + empty bypass list.
- **`CONTRIBUTING.md` §4 Branch Naming** — (a) removed the false-invalid example `feature/add-thing` (which actually DOES pass the regex: `add` = 2+ char `<dev>`, `thing` = 3+ char description) and replaced with genuinely invalid examples (`feature/ab-a`, `feature/roman--double-hyphen`, `random/roman-test`); (b) added a "Convention beyond the regex" note clarifying that `<dev>` is a human-readable short handle (typically first name / alias), not used for PR attribution — attribution is sourced from `${{ github.triggering_actor || github.actor }}` directly; (c) added a "Known contributor handles" table with GitHub handles + branch `<dev>` segments only (extended context such as legal names, tenure, and roles lives in internal onboarding docs, not this public repo); Nik's branch `<dev>` segment changed from `ncimino` to `nik` (shorter, matches first-name convention); placeholder TODO handles replaced with real ones (`@iamwaseem18`, `@mshahid538`); `@YonksTEAM` added as executive stakeholder reference; (d) added an "Enforcement posture" mini-matrix summarizing the three options documented in detail in ADR-003 so contributors understand why convention-not-mechanical is the current choice and what would trigger a change. Self-contained — no separate file.
- **`.github/workflows/README.md` header** — version bumped from `v3.3.4.1` to `v3.3.4.2` per #WeOwnVer pre-merge iteration policy (this is the second merge-event iteration in the same ISO week). *A subsequent bump to `v3.3.5.1` was applied in the same PR on 2026-04-27 once the work crossed into ISO week 5 of April — see [`[v3.3.5.1]`](#v3351--2026-04-27-to-2026-04-28) above.*
- **`.github/CODEOWNERS`** — replaced placeholder handles `@mohammed-TODO` / `@shahid-TODO` / `@dhruv-TODO` with confirmed real GitHub handles `@iamwaseem18` / `@mshahid538` / `@dhruvmalik007` (active per-path rules unchanged — still `@ncimino @romandidomizio` until 2026-05-15 handoff decisions are finalized). Added `@YonksTEAM` as an executive stakeholder reference in the header (not a path reviewer — avoids notification noise). Header PII minimization: legal names, tenure descriptors, and recovery-credential custody details removed from the public header; extended operational context lives in internal onboarding / security docs.
- **Branch naming example sync** — updated example branch `fix/ncimino-...` → `fix/nik-...` across `.github/workflows/branch-name-check.yml` (error-message output), `.github/workflows/README.md` §3, and root `README.md` §"Branch Strategy" to match Nik's confirmed branch `<dev>` segment in `CONTRIBUTING.md` §4. `auto-pr-to-main.yml` reviewer assignment remains `--add-reviewer ncimino,romandidomizio` — Nik's GitHub handle `@ncimino` is unchanged; only his `<dev>` branch segment changed from `ncimino` to `nik`.
- **`.github/workflows/README.md` §9 + §10** — Reviewer Rotation Procedure step 2 and Transition Checklist row 4 updated to reflect that placeholder-handle replacement is complete (`~~strikethrough~~ + ✅`), leaving only the per-path specialist-assignment decision as pending. Keeps the transition checklist an accurate audit artifact of what's done vs. still open.
- **`.github/INCIDENT_RESPONSE.md` Scenario 6** — Stewardship Gap response steps 2 and 3 updated to reference real GitHub handles (`@iamwaseem18` / `@mshahid538` / `@dhruvmalik007`) instead of `@<name>-TODO` placeholders. Incident playbook accuracy matters for SEV-4 on-call response — no ambiguity about who to page.
- **`CONTRIBUTING.md` §4** now explicitly documents the branch-name-vs-PR-body identifier split: `<dev>` is a human-readable short handle; PR `**Triggered by:**` attribution uses `${{ github.triggering_actor || github.actor }}` automatically. Added a blockquote callout table for quick reference. Resolves a user-facing ambiguity flagged in Copilot round-1 review of PR #13.
- **`.github/workflows/README.md` §3 Parsing Rules** simplified from 5 steps (including a handle-to-username mapping step) down to 3 steps (regex validation + direct `${{ github.triggering_actor || github.actor }}` attribution). Added a "Branch name vs. PR body—two different identifiers (by design)" subsection with a clarity table.
- **`.github/workflows/README.md` §9 Reviewer Rotation Procedure** — stripped contributor legal name in the "placeholder-handle replacement complete" note; kept only the GitHub handles (PII minimization in public runbook, addressing Copilot round-2 review feedback).
- **`.github/CODEOWNERS` PII minimization** — header re-written to remove contributor legal names, tenure descriptors, intern status, executive titles, and recovery-credential custody details. Kept only GitHub handles + minimal functional-area descriptors (IaC, Docker, Agentic AI). Reduces social-engineering surface area on a public repo, addressing Copilot round-2 review feedback.
- **Round-3 consistency + PII follow-ups** (Copilot review, 2026-04-24):
  - `.github/ADR-003-main-branch-ruleset.md` Option A regex example updated from `(roman\|ncimino\|mohammed\|...)` to `(roman\|nik\|mohammed\|...)` to match the current `<dev>` segment convention in `CONTRIBUTING.md` §4.
  - `.github/ADR-003-main-branch-ruleset.md` Option B rationale bullet rewritten: previously described a now-retired inline `<dev>` → GitHub-username mapping + git-author-email fallback; now correctly describes the `${{ github.triggering_actor || github.actor }}` platform-sourced attribution. Keeps the ADR a truthful control-evidence artifact.
  - `CONTRIBUTING.md` §4 — all three references (`<dev>` intro paragraph, blockquote callout table cell, blockquote explanatory paragraph, Known-contributor-handles onboarding paragraph) updated from shortform `github.actor` to the full `${{ github.triggering_actor || github.actor }}` expression to match the workflow env variable exactly.
  - `.github/workflows/README.md` §3 "Branch name vs. PR body" table cell updated to use the full `${{ github.triggering_actor || github.actor }}` expression to match the Parsing Rules step 3 above it.
  - `.github/workflows/README.md` §10 Transition Checklist row 2 softened: `Transfer 2FA (TOTP seed) + recovery codes to enterprise admin (Yonks) + rotation lead` → `Transfer 2FA administration per internal runbook to enterprise admin + rotation lead`. Removes specific credential-type details from a public runbook (social-engineering surface reduction).
- *Continuation rounds 4–7 (all 2026-04-27) — see [`[v3.3.5.1]`](#v3351--2026-04-27-to-2026-04-28) above for the canonical record. Includes the round-5 three-tier PR-body attribution refactor + `workflow_dispatch` + `concurrency`, the round-6 PII strip + ADR-004 creation + auto-trigger claim revert, and the round-7 Copilot 8-comment close-out + §2A + §11.*

### Fixed

- **`.github/ADR-003-main-branch-ruleset.md` stale path reference** — "Not enabled → Require signed tags" bullet previously pointed to `.pr7-handoff-checklist` (a local-only, gitignored file that does not exist in the repo tree). Replaced with `/CHANGELOG.md` + `docs/VERSIONING_WEOWNVER.md`, both of which are in-repo tracking locations. Resolves 1 of 6 Copilot review comments on PR #13.
- **`.github/ADR-003-main-branch-ruleset.md` date arithmetic** — "Next scheduled review" incorrectly read `2026-07-23`. 2026-04-23 + 90 calendar days = 2026-07-22 (April 30-23=7 remaining days in April, May=31, June=30 → 7+31+30=68 days, 90-68=22 → July 22). Corrected to `2026-07-22` and clarified "90 calendar days" to remove math ambiguity. Resolves 1 of 6 Copilot review comments on PR #13 about date inconsistency.

### Security / Compliance

- **SOC 2 CC6.3 evidence-ready**: §8.1 table maps each of the 12 enabled rules to specific SOC 2 Trust Services Criteria. Auditors can cross-reference the GitHub ruleset config with this table for one-shot coverage proof.
- **ISO 27001 A.5.15 + A.5.37**: path-reviewer binding (CODEOWNERS #3) + change-of-state reset (stale approval dismissal #2) documented and enforced.
- **ISO 42001 AI governance**: rule #10 (auto-request Copilot review) now mapped to Annex A.6.2.7 (AI-aware technical controls) and A.6.2.8 (review + approval of AI-generated output).
- **`WEOWN_BOT_PAT` rotation audit note** — the fine-grained PAT scoped to `WeOwnNetwork/ai` was regenerated on 2026-04-23 during this PR's cycle to restore the `auto-pr-to-main.yml` workflow (prior PAT was invalidated; `PAT Health Check` workflow run `24870414632` confirmed the post-rotation PAT authenticated as `weown-bot` with 89 days to next expiration on 2026-07-22). New PAT is 89-day fine-grained, single-repo scope, stored via Infisical → GitHub Secret sync. The rotation event itself is an auditable control (no sensitive credential material recorded here).

### Meta

- **Testing evidence**: all three workflows validated via a 42-case solo regression matrix (branch-name regex coverage, defense-in-depth parity, developer attribution extraction, jq fallback behavior, YAML parse) — see `.pr7-test-workspace/solo-tests.sh` (local-only, not committed).
- **Version cadence**: this section captures the Season 3, April Week 4 work (original PR + Copilot rounds 1–3). Subsequent same-PR work on 2026-04-27 crossed into Week 5 of April and is documented under [`[v3.3.5.1]`](#v3351--2026-04-27-to-2026-04-28) above. See `docs/VERSIONING_WEOWNVER.md` for the full cadence rules.

---

## [v3.3.4.1] — 2026-04-23

First repository-level CHANGELOG entry (#WeOwnVer `vSEASON.MONTH.WEEK.ITERATION` — Season 3, April, Week 4 of April, Iteration 1). Establishes auto-PR workflow hardening, ecosystem-wide service account, Infisical GitHub Sync, branch naming enforcement, and the initial compliance roadmap.

### Added

- **`weown-bot` ecosystem-wide GitHub service account** with per-repo fine-grained PATs, centralized in Infisical project `weown-bot GitHub PATs` (see [ADR-001](.github/ADR-001-service-account-pat.md))
- **Infisical → GitHub Sync integration** for `WEOWN_BOT_PAT__WEOWNNETWORK_AI` (see [ADR-002](.github/ADR-002-infisical-github-sync.md))
- **New workflows**:
  - [`.github/workflows/pat-health-check.yml`](.github/workflows/pat-health-check.yml) — scheduled weekly PAT health check; opens issue at ≤14 days; hard-fails at ≤3 days
  - [`.github/workflows/branch-name-check.yml`](.github/workflows/branch-name-check.yml) — blocks non-conforming branch names (enforces `<type>/<dev>-<description>` convention)
- **New documents**:
  - [`.github/ADR-001-service-account-pat.md`](.github/ADR-001-service-account-pat.md)
  - [`.github/ADR-002-infisical-github-sync.md`](.github/ADR-002-infisical-github-sync.md)
  - [`.github/SECURITY_ASSESSMENT.md`](.github/SECURITY_ASSESSMENT.md) — threat model, risk register, compliance mapping
  - [`.github/INCIDENT_RESPONSE.md`](.github/INCIDENT_RESPONSE.md) — SEV-1..4 runbooks for PAT/account/Infisical/stewardship scenarios
  - [`.github/CODEOWNERS`](.github/CODEOWNERS) — path-based reviewer assignment with post-2026-05-15 handoff TODOs
  - [`.github/workflows/README.md`](.github/workflows/README.md) — authoritative ops reference (usage table, rotation procedure, alert stack, transition checklist, branch protection, replication steps)
  - [`docs/COMPLIANCE_ROADMAP.md`](docs/COMPLIANCE_ROADMAP.md) — detailed 5-phase compliance roadmap (NIST CSF → CIS → CSA CCM → ISO 27001 → SOC 2 → ISO 42001) with CI/CD integration per phase, success metrics, and forward-looking guardrails
  - [`CONTRIBUTING.md`](CONTRIBUTING.md) — first-time developer onboarding. Covers mandatory SSH-based commit signing (NIST PR.DS-6, SOC 2 CC7.1, ISO 27001 A.8.28, CIS 16.12) with 7-step setup including `gpg.ssh.allowedSignersFile` for local verification and verified-email alignment with GitHub account; the **GitHub Flow** branching model (explicitly named + lifecycle diagram + what we do NOT use + rationale table + branch lifetime expectations); branch naming convention; commit message conventions; full PR workflow with post-merge cleanup; review expectations; expanded troubleshooting (signature-present-vs-absent pre-check, 6-cause diagnostic for `%G?=N`, 2-cause diagnostic for GitHub "Unverified" including committer-email mismatch, retro-sign-and-force-push recipe, ssh-agent/keychain fixes); and compliance cross-references
  - [`CHANGELOG.md`](CHANGELOG.md) — this file
- **PR body enhancements** (in `auto-pr-to-main.yml`):
  - NIST CSF Function-aligned human review checklist (Govern, Identify, Protect, Detect, Respond, Recover)
  - Full commit bodies visible to Copilot for context (`%b` not just `%s`)
  - Author identity (`%an <%ae>`) and date (`%ad`) shown per commit
  - Developer attribution parsed from branch naming convention (`<type>/<dev>-<description>`)
  - Deduplicated contributor list
- **Auto-assignment of 2 reviewers** (`@ncimino` + `@romandidomizio`) via `gh pr edit --add-reviewer`

### Changed

- **`auto-pr-to-main.yml`**: token reference `ROMAN_PAT` → `WEOWN_BOT_PAT`
- **`.github/copilot-instructions.md`**: full rewrite — removed command/test directives (Copilot is static-only), added phase-aware compliance, expanded §3 checklist to cover all six frameworks (NIST CSF 2.0, CIS v8 IG1, CSA CCM v4, ISO/IEC 27001:2022, SOC 2 TSC, ISO/IEC 42001:2023) plus seven ecosystem best-practice blocks (Kubernetes, Docker/Compose, **IaC [OpenTofu for infrastructure + Ansible for software/config]**, Infisical, Observability, GitOps, Security/Supply Chain), added §3.0 explicit PUBLIC-repo precautions (never-commit list, placeholder patterns, git history hazards), moved checklist to top of document, added forward-looking guardrails, ecosystem awareness, anti-pattern reference, and cross-framework review output guidelines. Ansible integration documented throughout: Infisical secrets via `community.hashi_vault` / `infisical run -- ansible-playbook`; idempotency / handlers / vault / inventory best practices; anti-patterns for plaintext secrets, non-idempotent shell, implicit root
- **`.github/CI_CD_WORKFLOWS.md`**: cross-referenced the new workflows README, ADRs, and compliance roadmap; documented `pat-health-check.yml`
- **`docs/VERSIONING_WEOWNVER.md`**: rewritten to the corrected calendar-driven methodology (L-094 REVISED): `vSEASON.MONTH.WEEK.ITERATION`, L-115 ISO-week-offset rule, finalized Season Calendar, Helm/OCI mapping, and calculation cheat sheet
- **`README.md`** (top-level): added "Compliance & Governance" section linking all new docs

### Removed

- **`.github/PAT_MIGRATION_GUIDE.md`** deleted — content was superseded by ADR-001 (rationale), ADR-002 (Infisical sync), CHANGELOG (history), and `.github/workflows/README.md` (authoritative rotation procedure). Retaining the old guide created duplicate-source risk.
- **`maintenance` branch** removed from `auto-pr-to-main.yml` triggers and all documentation. The repository now standardizes on **GitHub Flow**: short-lived `feature/*`, `fix/*`, `docs/*`, `hotfix/*` branches off `main`, merged back via reviewed PRs.

### Security

- Fine-grained PAT replaces broad-scoped tokens: minimally scoped to `Contents: Read` + `Pull requests: R/W` + metadata (auto) on `WeOwnNetwork/ai`. Issue creation in `pat-health-check.yml` intentionally uses the ephemeral per-run `GITHUB_TOKEN` (with workflow-level `issues: write`) rather than expanding the PAT — principle of least privilege (NIST PR.AC-3 / CIS 5.4)
- Secret management centralized in Infisical with 90-day audit logs (SOC 2 evidence)
- Branch naming enforced by `branch-name-check.yml` (blocks non-conforming branches via required status check). Description segment requires 3+ alphanumeric chars before any hyphen suffix (e.g., `feature/ab-a` now rejected). Regex kept in sync with the defense-in-depth guard in `auto-pr-to-main.yml`
- Branch protection to be configured: require 2 approvals + review from Code Owners + signed commits + no bypass (see [`.github/workflows/README.md` §8](.github/workflows/README.md#8-required-branch-protection-settings))
- **Workflow hardening (Copilot review rounds 3–5)**:
  - `pat-health-check.yml` now **fails closed** (`exit 1`) when the `github-authentication-token-expiration` header is missing — previously silently exited 0, which defeated the workflow's safety-net purpose. A missing header indicates token-type misconfiguration (e.g., classic PAT instead of fine-grained) and must surface as a red-X in Actions
  - `pat-health-check.yml` now **fails closed** (`exit 1`) when the header IS present but the timestamp cannot be parsed into an epoch — previously emitted a `::warning::` and silently exited 0, creating a second bypass path that would activate exactly when GitHub changed the header format. Error message includes the offending raw value for forensics (round 4)
  - `pat-health-check.yml` separates `gh api /user` exit-code check from header-grep so a transient API/network failure (previously swallowed by `2>/dev/null | grep ... || true`) is not misclassified as a PAT-configuration issue. Three distinct red-X paths now exist with targeted error messages: (1) API call itself failed, (2) call succeeded but header absent, (3) header present but unparseable. Stderr of the failing `gh api` is surfaced in the Actions log (round 5)
  - `branch-name-check.yml` sets `permissions: {}` — the workflow makes no API calls and does not check out the repo (only reads `github.head_ref` / `github.ref` context and runs a local grep), so the ephemeral `GITHUB_TOKEN` is stripped of every permission (round 5)
  - `pat-health-check.yml` removed unused `ISSUE_LABELS` variable — labels are passed directly to `gh issue create` via three explicit `--label` flags; the unused variable falsely implied a single source of truth (round 4)
  - Temp files in both workflows now route through `$RUNNER_TEMP` (GitHub-runner-scoped, auto-cleaned at job end) instead of the shared `/tmp` — defense in depth beyond the existing `mktemp` + `trap` cleanup pattern
  - `pat-health-check.yml` issue-body links use `${{ github.server_url }}` instead of hardcoded `https://github.com` — portable to GitHub Enterprise Server (matches the `BLOB_BASE` pattern already in `auto-pr-to-main.yml`)
  - `auto-pr-to-main.yml` PR-existence check uses jq `.[0].number // empty` — avoids jq's literal `"null"` string on empty arrays, which would previously cause the script to attempt `gh pr edit null`
  - `docs/VERSIONING_WEOWNVER.md` Helm chart mapping corrected for SemVer precedence: every iteration gets a `-N` prerelease suffix (`3.3.4-1 < 3.3.4-2 < 3.3.4-3 < 3.3.4`), preventing the SemVer-downgrade pitfall where `3.3.4-2` would sort BELOW `3.3.4` in Helm/OCI tooling

### Compliance

- **NIST CSF 2.0** — `Govern`, `Protect (Access Control, Data Security)`, `Detect`, `Respond` functions addressed for auto-PR workflow
- **CIS Controls v8 IG1** — Controls 3, 5, 6, 7, 8, 13, 16, 17 in scope
- **ISO/IEC 27001:2022** — A.5.15 (access control), A.5.37 (documented operating procedures), A.8.2, A.8.24 (cryptographic/secret mgmt), A.8.32 (change mgmt)
- **SOC 2 TSC** — CC6.1–CC6.3 (logical access), CC7.1–CC7.2 (system operations), CC8.1 (change mgmt)

### Transition Note (2026-05-15)

`@romandidomizio` departs 2026-05-15. PAT rotation responsibility transitions to one of Mohammed / Shahid / Dhruv. Full handoff checklist in [`.github/workflows/README.md` §10](.github/workflows/README.md#10-transition-checklist-2026-05-15).

---

## Links

- [Repository README](README.md)
- [Compliance Roadmap](docs/COMPLIANCE_ROADMAP.md)
- [Workflows Documentation](.github/workflows/README.md)
- [Copilot Instructions](.github/copilot-instructions.md)
- [Versioning Standard](docs/VERSIONING_WEOWNVER.md)
