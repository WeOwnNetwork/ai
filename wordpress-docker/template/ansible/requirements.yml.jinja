# Pinned Ansible collections required by deploy.yml
# Install with: ansible-galaxy collection install -r requirements.yml
---
collections:
  - name: community.docker
    version: "3.10.4"
  - name: ansible.posix
    version: "1.5.4"
