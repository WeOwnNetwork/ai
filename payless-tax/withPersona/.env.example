# Copy this file to .env and fill in values where needed.

# MySQL settings used by the app container.
# In Docker, DB_HOST is overridden to `db` via docker-compose.
DB_HOST=localhost
DB_PORT=3306
DB_USER=payless_user
DB_PASSWORD=payless_password
DB_NAME=payless_tax

# Persona sandbox API key
# Example: persona_sandbox_xxx... (never commit your real key)
PERSONA_API_KEY="persona_sandbox_xxxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Persona inquiry template id (sandbox)
# Create a KYC template in Persona that includes:
#   - Government ID (passport)
#   - Personal details collection
#   - Watchlist / sanctions / AML checks
# Then copy its inquiry template id (starts with itmpl_...).
PERSONA_INQUIRY_TEMPLATE_ID=itmpl_xxxxxxxxxxxxxxxxx
