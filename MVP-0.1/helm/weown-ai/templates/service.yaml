apiVersion: v1
kind: Service
metadata:
  name: {{ include "weown-ai.fullname" . }}
  labels:
    {{- include "weown-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weown-ai.selectorLabels" . | nindent 4 }}
